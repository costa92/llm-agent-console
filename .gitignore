# Go build artifacts
/console
/tmp/
*.test
*.out

# SPA build output (served by nginx; not committed)
/web/dist/

# Local secret overrides (the committed sample is config/config.dev.yaml)
/config/config.local.yaml
/config/config.prod.yaml
