---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - web/src/components/primitives/FiveStateWrapper.tsx
  - web/src/components/primitives/RawJsonViewer.tsx
  - web/src/components/primitives/CopyableId.tsx
  - web/src/test/FiveStateWrapper.test.tsx
  - web/src/test/RawJsonViewer.test.tsx
  - web/src/test/CopyableId.test.tsx
autonomous: true
requirements:
  - SHELL-05
  - SHELL-06
  - SHELL-07

must_haves:
  truths:
    - "FiveStateWrapper renders a spinner + 'Loading...' when loading=true"
    - "FiveStateWrapper renders red alert-circle + upstream error message when error is set"
    - "FiveStateWrapper renders EmptyState with 'No operator context set' copy when empty=true"
    - "FiveStateWrapper renders amber partial banner + children when partial is set"
    - "FiveStateWrapper renders children when all states are false (ready state)"
    - "RawJsonViewer is collapsed by default; expands on toggle; copy button copies JSON to clipboard"
    - "CopyableId renders id in monospace; copy icon reveals on hover; click copies to clipboard and shows check icon for ~1s"
    - "All five states are visually distinct (no blank/ambiguous panel)"
  artifacts:
    - path: "web/src/components/primitives/FiveStateWrapper.tsx"
      provides: "loading/empty/error/partial/ready states per UI-SPEC five-state contract"
    - path: "web/src/components/primitives/RawJsonViewer.tsx"
      provides: "collapsible JSON viewer with copy-to-clipboard and syntax coloring"
    - path: "web/src/components/primitives/CopyableId.tsx"
      provides: "monospace id with hover-reveal copy icon + check icon + toast on click"
  key_links:
    - from: "FiveStateWrapper error prop"
      to: "ErrorState component"
      via: "status + service + message from upstream BFF-04 passthrough"
      pattern: "error.message"
    - from: "RawJsonViewer copy button"
      to: "navigator.clipboard.writeText"
      via: "async clipboard API"
      pattern: "clipboard"
    - from: "CopyableId copy click"
      to: "toast.success('Copied')"
      via: "sonner toast"
      pattern: "toast.success"
---

<objective>
Build the cross-cutting UI primitives that all later phases reuse: FiveStateWrapper (five-state contract), RawJsonViewer (collapsible JSON + clipboard), and CopyableId (mono id + one-click copy).

Purpose: Every list/detail/stream view in Phases 2-5 renders exactly one of the five states — never a blank panel. These primitives enforce that contract and are built once here.

Output: Three primitive components with unit tests covering all state variants. After this plan, the full Phase 1 vertical slice is complete and the phase gate can be verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<interfaces>
From web/src/components/ui/ (shadcn components installed in Plan 02):
  Collapsible, CollapsibleContent, CollapsibleTrigger
  Button (variant="ghost" for icon-only buttons)
  Badge (for status labels)

From sonner (installed in Plan 02):
  import { toast } from 'sonner'
  toast.success("Copied")  — auto-dismisses ~4s
  toast.error("...")        — persists until dismissed

From lucide-react (installed in Plan 02):
  Loader, AlertCircle, AlertTriangle, CheckCircle, Copy, Check, ChevronDown, ChevronRight
  All at size={16} (size-4) per UI-SPEC icon default

UI-SPEC five-state copy (exact strings per Copywriting Contract):
  loading state: "Loading..." with Loader spin icon
  empty state heading: "No operator context set"
  empty state body: "Set a tenant id and user id to reach the backends. Project and session are optional."
  error state: "{HTTP status} from {service} — {upstream message}."
  partial state banner: "Showing partial data — {what is missing/why}."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FiveStateWrapper — all five states with UI-SPEC-compliant copy and icons</name>
  <files>web/src/components/primitives/FiveStateWrapper.tsx, web/src/test/FiveStateWrapper.test.tsx</files>

  <read_first>
    - `.planning/phases/01-foundation/01-UI-SPEC.md` The five-state pattern section (exact state table, visual specs for each state), Cross-cutting primitive contracts (FiveStateWrapper as reusable primitive), Copywriting Contract (exact copy strings for each state — loading, empty, error, partial)
    - `.planning/phases/01-foundation/01-RESEARCH.md` Pattern 7 (FiveStateWrapper TypeScript interface: FiveStateProps with loading/error/empty/partial/children; ErrorState renders status+service+message; LoadingState renders spinner + "Loading…")
    - `.planning/phases/01-foundation/01-UI-SPEC.md` Color section — error uses --status-down red; partial uses --status-degraded amber; loading uses --status-unknown slate
  </read_first>

  <behavior>
    - Test: when loading=true, renders lucide Loader icon and text "Loading…"; does NOT render children
    - Test: when error={status:503, service:'memory-gateway', message:'upstream unavailable'}, renders "503 from memory-gateway — upstream unavailable." with AlertCircle icon; does NOT render children
    - Test: when empty=true, renders heading "No operator context set" and body copy about tenant/user; does NOT render children; renders primary Button "Set context"
    - Test: when partial={message:'health check for flowd timed out'}, renders amber alert-triangle + "Showing partial data — health check for flowd timed out." AND renders children alongside the banner
    - Test: when loading=false, error=null, empty=false, partial=null, renders only children (ready state)
    - Test: error state renders a "View raw JSON" disclosure element (collapsed by default) — uses RawJsonViewer or similar collapsible
  </behavior>

  <action>
    Create `web/src/components/primitives/FiveStateWrapper.tsx` with the `FiveStateProps` type from RESEARCH.md Pattern 7: `{ loading: boolean; error?: { status?: number; service?: string; message: string } | null; empty?: boolean; partial?: { message: string } | null; children: React.ReactNode }`.

    Implement each sub-state as a named sub-component within the file:
    - `LoadingState`: centered layout with `Loader` icon (animate-spin, size-4, color `--status-unknown`) and "Loading…" body text (Body 14px, muted).
    - `ErrorState({ status, service, message })`: `AlertCircle` icon (size-4, color `--status-down`), followed by the exact copy pattern from UI-SPEC: `"{status} from {service} — {message}."` — if status is undefined, omit it; if service is undefined, omit "from {service}". Below: muted "Retry" button (ghost variant) and a "View raw JSON" collapsible disclosure (collapsed by default, using shadcn Collapsible with a ChevronRight/ChevronDown toggle). The collapsible content renders the raw error object as JSON in a `<pre>` with mono font.
    - `EmptyState`: centered, icon (e.g. `Circle` or `Inbox`), heading "No operator context set" (Heading 16px, semibold), body copy "Set a tenant id and user id to reach the backends. Project and session are optional." (Body 14px), a primary `Button` "Set context" (blue accent per UI-SPEC).
    - `PartialBanner({ message })`: amber `AlertTriangle` (size-4, color `--status-degraded`) + "Showing partial data — {message}." in Body text. Renders as a banner ABOVE children, not replacing them.

    The component returns:
    - `if (loading)` → `<LoadingState />`
    - `else if (error)` → `<ErrorState {...error} />`
    - `else if (empty)` → `<EmptyState />`
    - `else` → `<>{partial && <PartialBanner {...partial} />}{children}</>`

    Create `web/src/test/FiveStateWrapper.test.tsx` covering all six behavior cases. Use `@testing-library/react` `render` and `screen.getByText`/`getByRole`. For the Retry button in error state: test it exists; do NOT test click behavior (Retry logic is a consumer concern, not the primitive's).
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run --reporter=verbose src/test/FiveStateWrapper.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/test/FiveStateWrapper.test.tsx` exits 0; all 6 test cases green
    - `grep -c 'Loading' web/src/components/primitives/FiveStateWrapper.tsx` returns >= 1 (loading copy present)
    - `grep -c 'No operator context set' web/src/components/primitives/FiveStateWrapper.tsx` returns 1 (exact UI-SPEC empty-state heading)
    - `grep -c 'from {' web/src/components/primitives/FiveStateWrapper.tsx` returns 0 — the copy uses template literals/JSX expressions, not a literal `{`
    - `grep -c 'Showing partial data' web/src/components/primitives/FiveStateWrapper.tsx` returns 1 (partial banner copy present)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>FiveStateWrapper: all five states render with UI-SPEC-compliant copy and icons; loading/error/empty/partial are visually distinct; partial banner renders above children; error includes raw-JSON disclosure; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RawJsonViewer + CopyableId primitives</name>
  <files>web/src/components/primitives/RawJsonViewer.tsx, web/src/components/primitives/CopyableId.tsx, web/src/test/RawJsonViewer.test.tsx, web/src/test/CopyableId.test.tsx</files>

  <read_first>
    - `.planning/phases/01-foundation/01-UI-SPEC.md` Cross-cutting primitive contracts — Raw-JSON viewer contract (collapsible, syntax-highlighted, copy-to-clipboard, collapsed by default; "Copied" toast; mono font; muted punctuation, neutral keys); Copyable-id contract (mono, hover-reveal copy icon, flips to check for ~1s, toasts "Copied", min 32px hit target)
    - `.planning/phases/01-foundation/01-UI-SPEC.md` Typography section (mono font class from index.css), spacing section (icon-only buttons: 32px minimum hit target), Color section (muted punctuation: --muted-foreground)
    - `.planning/phases/01-foundation/01-RESEARCH.md` Standard Stack Supporting section (syntax highlighting note in UI-SPEC: lightweight owned highlighter or shiki/prism as normal npm dep — do NOT use shadcn registry; for Phase 1 a plain `<pre>` with mono font is sufficient; defer syntax colorization to a follow-up)
  </read_first>

  <behavior>
    - Test (RawJsonViewer): collapsed by default — JSON content is NOT visible in the DOM before toggle
    - Test (RawJsonViewer): after clicking the toggle button, the JSON content is visible
    - Test (RawJsonViewer): the copy button calls navigator.clipboard.writeText with the stringified JSON
    - Test (RawJsonViewer): after copy, toast.success is called with "Copied"
    - Test (CopyableId): renders the id value in a monospace element
    - Test (CopyableId): clicking the copy icon calls navigator.clipboard.writeText with the id value
    - Test (CopyableId): after copy, toast.success is called with "Copied" and the icon switches to Check
  </behavior>

  <action>
    Create `web/src/components/primitives/RawJsonViewer.tsx` with props `{ data: unknown; label?: string }`. Use shadcn `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent` for expand/collapse. The trigger shows a `ChevronRight` icon when collapsed and `ChevronDown` when expanded, plus optional `label` text (default "Raw JSON"). Collapsed by default (`open` state initializes to `false`). When expanded, renders `JSON.stringify(data, null, 2)` inside a `<pre>` element with the `.mono` CSS class from index.css, `text-xs` (12px per Label size), `--muted-foreground` color, and `--card` background with rounded corners and padding. No external syntax highlighting library in Phase 1 — plain formatted JSON is sufficient. The copy button (ghost `Button` variant, `Copy` icon from lucide-react, no visible label — icon-only) is always visible (not just when expanded); clicking it: (1) calls `navigator.clipboard.writeText(JSON.stringify(data, null, 2))`; (2) calls `toast.success("Copied")`; (3) temporarily swaps the `Copy` icon for `Check` for 1 second then reverts. The copy button has min 32px × 32px hit target (padding: `p-1` or equivalent per UI-SPEC interactive hit target rule).

    Create `web/src/components/primitives/CopyableId.tsx` with props `{ id: string; className?: string }`. Renders the id in an inline `<span>` with the `.mono` CSS class (14px Body size per UI-SPEC). A `Copy` icon from lucide-react appears on hover (CSS `opacity-0 group-hover:opacity-100` on the icon; wrap the outer element with `className="group"`). Min 32px hit target on the icon button. On click: (1) `navigator.clipboard.writeText(id)`; (2) `toast.success("Copied")`; (3) swap icon to `Check` for 1 second then revert using `useState` + `setTimeout`.

    Create test files `web/src/test/RawJsonViewer.test.tsx` and `web/src/test/CopyableId.test.tsx`. Mock `navigator.clipboard` with `vi.stubGlobal('navigator', { clipboard: { writeText: vi.fn().mockResolvedValue(undefined) } })`. Mock `sonner`'s `toast.success` with `vi.mock('sonner', ...)`. For the collapse test, query by role or test ID before and after the toggle click.
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run --reporter=verbose src/test/RawJsonViewer.test.tsx src/test/CopyableId.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/test/RawJsonViewer.test.tsx` exits 0; all 4 behavior cases green (collapsed by default, expand on toggle, copy writes to clipboard, toast called)
    - `npx vitest run src/test/CopyableId.test.tsx` exits 0; all 3 behavior cases green (mono render, clipboard write on click, Check icon + toast)
    - `grep -c 'Collapsible' web/src/components/primitives/RawJsonViewer.tsx` returns >= 1 (uses shadcn Collapsible)
    - `grep -c 'mono' web/src/components/primitives/RawJsonViewer.tsx` returns >= 1 (mono font class applied)
    - `grep -c 'mono' web/src/components/primitives/CopyableId.tsx` returns >= 1 (mono font class applied to id span)
    - `grep -c 'navigator.clipboard' web/src/components/primitives/RawJsonViewer.tsx` returns >= 1
    - `grep -c 'navigator.clipboard' web/src/components/primitives/CopyableId.tsx` returns >= 1
    - `grep -c 'toast.success' web/src/components/primitives/CopyableId.tsx` returns >= 1 ("Copied" toast)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>RawJsonViewer: collapsed by default, expand on toggle, copy-to-clipboard with "Copied" toast, mono pre with muted color. CopyableId: mono inline, hover-reveal copy icon, check icon for 1s after copy, "Copied" toast. Both unit test suites green. Phase 1 all cross-cutting primitives complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → navigator.clipboard | Browser clipboard write API; triggered only by explicit user click |
| RawJsonViewer data prop → DOM | Operator-provided data rendered in pre element; not eval'd, not injected as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | RawJsonViewer renders upstream JSON in DOM | mitigate | JSON is rendered via `JSON.stringify()` into a `<pre>` text node, NOT via `dangerouslySetInnerHTML`; no XSS vector from upstream JSON content |
| T-05-02 | Information Disclosure | RawJsonViewer copies full JSON to clipboard | accept | Clipboard access requires explicit user gesture (click); no auto-copy; the "Copied" toast confirms the action; acceptable for an internal operator tool |
| T-05-03 | Information Disclosure | ErrorState RawJsonViewer disclosure reveals upstream error body | accept | BFF-04 pass-through is the intended behavior (operator needs to see the real upstream error); error body is shown only after an explicit click on "View raw JSON"; internal tool, no end-user risk |
| T-05-SC | Tampering | No new npm packages in this plan | accept | All packages used (sonner, lucide-react, shadcn collapsible/button) were installed and verified in Plan 02 Package Legitimacy Audit; no new installs |
</threat_model>

<verification>
## Phase 1 Full Gate (after Wave 4)

Run after all five plans complete to confirm the full phase-1 gate:

1. `go test ./... -count=1` — all Go unit tests PASS (includes TestErrorPassthrough, TestFlowDirectorResponseNoToken from Plan 03)
2. `cd web && npx vitest run` — all Vitest tests PASS (includes Toast.test.tsx from Plan 04)
3. `go build -o /tmp/console ./cmd/console && ./scripts/sse-proof.sh` — exits 0; PART 1 (direct BFF on :8090 >= 3 tick events) AND PART 2 (through nginx on :80 via docker-compose, gzip compression on, >= 3 tick events) both succeed — this is the actual BFF-03 gate per D-06 and ROADMAP SC4
4. `grep -rn 'Bearer\|FlowdToken\|flowd_token' web/src/` — returns 0 lines (no BFF secrets in SPA source)
5. `grep -rn 'Bearer\|FlowdToken' web/dist/ 2>/dev/null` — returns 0 lines (no BFF secrets in SPA build output; run after `npm run build`)
6. `grep -c 'proxy_buffering off' deploy/nginx.conf` — >= 1
7. `grep -c 'gzip off' deploy/nginx.conf` — >= 1
8. `grep -c 'proxy_read_timeout 3600s' deploy/nginx.conf` — 1 (on SSE location)
9. SSE location line number < general /api/ location line number in deploy/nginx.conf
10. `go test ./internal/proxy/... -run TestConfigEnv -v` — /api/config/env response does NOT contain flowd_token or operator_token value
11. `go test ./internal/proxy/... -run TestErrorPassthrough -v` — 422 and 503 upstream status codes + bodies forwarded verbatim
12. `go test ./internal/proxy/... -run TestFlowDirectorResponseNoToken -v` — flowd token absent from client-side response headers and body
13. `cd web && npx vitest run src/test/Toast.test.tsx` — SHELL-06: success toast has no "Copy error"; error toast has "Copy error" affordance
14. `grep -c 'fetchEventSource' web/src/lib/sse.ts` — >= 1 (Phase 3/4 SSE stub present)
15. `cd web && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- FiveStateWrapper: all five states render with UI-SPEC-compliant copy, icons, and colors; no blank/ambiguous panels
- RawJsonViewer: collapsed default, expand toggle, copy-to-clipboard, "Copied" toast, mono pre
- CopyableId: mono id, hover-reveal copy icon, check icon for 1s after copy, "Copied" toast
- All Vitest tests green across all primitives + Toast.test.tsx + NavBar.test.tsx + OperatorContext.test.tsx
- Phase 1 full gate (verification section above) passes end-to-end, including the through-nginx SSE proof and the new BFF security tests
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
