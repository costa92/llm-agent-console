---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/index.html
  - web/src/main.tsx
  - web/src/index.css
  - web/vitest.config.ts
  - web/src/test/setup.ts
autonomous: true
requirements:
  - SHELL-01

must_haves:
  truths:
    - "TypeScript version is exactly 5.9.3 (NOT 6.x) in package.json devDependencies"
    - "Tailwind v4 is configured via @tailwindcss/vite plugin in vite.config.ts — no tailwind.config.js exists"
    - "index.css starts with @import tailwindcss (v4 syntax, not @tailwind directives)"
    - "components.json exists (shadcn init ran successfully)"
    - "npx vitest run exits 0 on the scaffold (setup file loads)"
    - "Vite dev proxy routes /api to http://localhost:8090"
  artifacts:
    - path: "web/package.json"
      provides: "Pinned dependencies with typescript@5.9.3 explicit in devDependencies"
    - path: "web/vite.config.ts"
      provides: "Vite config with @tailwindcss/vite plugin and /api dev proxy to :8090"
    - path: "web/src/index.css"
      provides: "Tailwind v4 @import + dark-theme CSS variable tokens matching UI-SPEC"
    - path: "web/vitest.config.ts"
      provides: "Vitest config with jsdom environment and setup file"
    - path: "web/src/test/setup.ts"
      provides: "@testing-library/jest-dom matchers imported"
    - path: "web/src/main.tsx"
      provides: "React 19 + TanStack Router RouterProvider + QueryClientProvider entry"
    - path: "web/components.json"
      provides: "shadcn configuration (proves init ran)"
  key_links:
    - from: "web/vite.config.ts proxy /api"
      to: "http://localhost:8090"
      via: "Vite dev server proxy"
      pattern: "8090"
    - from: "web/src/index.css line 1"
      to: "Tailwind v4 engine"
      via: "@import tailwindcss"
      pattern: "@import .tailwindcss."
---

<objective>
Scaffold the React 19 + Vite SPA with the correct pinned stack, Tailwind v4 configuration, shadcn/ui initialization, and Vitest test harness.

Purpose: Create the SPA foundation that all later plans build on. Getting the Tailwind v4 / TypeScript 5.9.3 / shadcn 4.x setup correct at scaffold time avoids mid-phase breakage from version conflicts.

Output: `web/` directory with all dependencies installed, Tailwind v4 dark-theme tokens applied, shadcn initialized, Vitest scaffold green, Vite dev proxy wired to BFF :8090.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPA scaffold — Vite + React 19 + TypeScript 5.9.3 + Tailwind v4 + shadcn init</name>
  <files>web/package.json, web/vite.config.ts, web/tsconfig.json, web/index.html, web/src/index.css</files>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` Standard Stack SPA table, Installation Commands section, Common Pitfalls 3 (TypeScript 6.0 installed instead of 5.9.3), Common Pitfalls 4 (Tailwind v4 PostCSS instead of Vite plugin), Common Pitfalls 5 (shadcn add before init), Pattern 5 (Vite config with tailwindcss() plugin and dev proxy), Version Gotcha TypeScript and shadcn CLI sections
    - `.planning/phases/01-foundation/01-UI-SPEC.md` Design System table, Color section (exact CSS variable token values), Typography section (font stack strings)
  </read_first>

  <action>
    Scaffold inside `web/` using `npm create vite@latest . -- --template react-ts`. This generates package.json, vite.config.ts, tsconfig.json, index.html, src/main.tsx, src/App.tsx.

    Pin TypeScript in package.json devDependencies to exactly `"typescript": "5.9.3"` — the scaffold may install 6.0.3; override explicitly. Install all deps with exact versions from RESEARCH.md Installation Commands: tailwindcss@4.3.0, @tailwindcss/vite@4.3.0, @tanstack/react-router@1.170.11, @tanstack/react-query@5.101.0, @microsoft/fetch-event-source@2.0.1, sonner@2.0.7, lucide-react@1.17.0, react-hook-form@7.77.0, zod@4.4.3. Dev deps: vitest@4.1.8, @testing-library/react@16.3.2, @testing-library/jest-dom, @tanstack/react-query-devtools@5.101.0, typescript@5.9.3.

    Rewrite `vite.config.ts` per RESEARCH.md Pattern 5: import `tailwindcss` from `@tailwindcss/vite`; add `tailwindcss()` to plugins. Add `server.proxy` with key `/api` targeting `http://localhost:8090`, `changeOrigin: true`. Do NOT add a rewrite function — the BFF expects the `/api` prefix intact. Do NOT create `tailwind.config.js`.

    Replace `web/src/index.css` entirely: first line must be `@import "tailwindcss";` (Tailwind v4 syntax). Then define a `:root {}` CSS custom property block with the dark-theme token values from UI-SPEC: `--background: #0B0E14`, `--foreground: #E6EAF0`, `--card: #151A23`, `--border: #232A36`, `--primary: #3B82F6`, `--destructive: #EF4444`, `--status-up: #22C55E`, `--status-degraded: #F59E0B`, `--status-down: #EF4444`, `--status-unknown: #8A93A3`, `--muted-foreground: #8A93A3`. Set `html, body { background-color: var(--background); color: var(--foreground); margin: 0; height: 100%; }`. Add `.mono { font-family: ui-monospace, "JetBrains Mono", "SF Mono", "Cascadia Code", Menlo, Consolas, monospace; }` class for use on ids, env strings, and JSON content per UI-SPEC Typography.

    Update `tsconfig.json`: set `strict: true`, `target: "ES2022"`, `moduleResolution: "bundler"`, `jsx: "react-jsx"`.

    Run shadcn init: `npx shadcn@4.10.0 init` — choose New York style, zinc base color (for dark compatibility per UI-SPEC). After init, add Phase 1 components: `npx shadcn@4.10.0 add button card dialog popover input label tooltip badge separator scroll-area collapsible sonner`. These land in `src/components/ui/`.

    Delete generated `src/App.tsx` and `src/App.css` (replaced by shell components in Plan 04).
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && node -e "const p=require('./package.json');const v=p.devDependencies&&p.devDependencies.typescript;if(v!=='5.9.3')throw new Error('TS wrong: '+v);console.log('TS OK: '+v);" && grep -c "tailwindcss" vite.config.ts && test -f components.json && echo "shadcn OK"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "require('./web/package.json').devDependencies.typescript"` prints exactly `5.9.3`
    - `grep -c 'tailwindcss' web/vite.config.ts` returns >= 1 (plugin imported and used)
    - `grep -c 'tailwind.config' web/vite.config.ts` returns 0 (no v3 config reference)
    - First non-comment line of `web/src/index.css` contains `@import "tailwindcss"`
    - `test -f web/components.json` exits 0 (shadcn init ran)
    - `test -d web/src/components/ui` exits 0 (shadcn components installed)
    - `grep -c '8090' web/vite.config.ts` returns >= 1 (dev proxy to BFF port)
    - No `tailwind.config.js` or `tailwind.config.ts` file exists at `web/` root
    - `grep -c '@tailwind base' web/src/index.css` returns 0 (v3 directives not present)
  </acceptance_criteria>

  <done>Vite scaffold complete with TypeScript 5.9.3 pinned, Tailwind v4 via plugin, shadcn initialized, dark-theme CSS tokens defined, dev proxy to BFF :8090.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest + test harness scaffold + TanStack Router entry</name>
  <files>web/vitest.config.ts, web/src/test/setup.ts, web/src/main.tsx</files>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` Standard Stack Supporting table (Vitest 4.1.8, @testing-library/react 16.3.2), Validation Architecture section (config file, quick run command, Wave 0 Gaps list)
    - `.planning/phases/01-foundation/01-VALIDATION.md` Test Infrastructure table, Wave 0 Requirements
    - `.planning/phases/01-foundation/01-RESEARCH.md` Architecture Patterns section (recommended file `web/src/main.tsx` as React entry with QueryClientProvider and RouterProvider)
  </read_first>

  <action>
    Create `web/vitest.config.ts` configuring Vitest to use the jsdom environment (required for DOM-based component tests), reference the setup file at `src/test/setup.ts`, include all `src/**/*.test.{ts,tsx}` files, and use the `@vitejs/plugin-react` transform. Set `globals: true` so tests can use `describe`/`it`/`expect` without imports.

    Create `web/src/test/setup.ts` that imports `@testing-library/jest-dom` (provides custom matchers like `toBeInTheDocument`, `toHaveTextContent`). This file is referenced by vitest.config.ts's `setupFiles`.

    Rewrite `web/src/main.tsx` as the application entry point: create a `QueryClient` instance with default options; create a `Router` from TanStack Router (using a minimal root route for now — the full route tree is built in Plan 04); render `React.StrictMode` > `QueryClientProvider` > `RouterProvider`. Import `./index.css`. The route tree used here is a placeholder that renders a loading indicator — Plan 04 replaces it with the full shell and named routes.

    Create a placeholder smoke test at `web/src/test/smoke.test.ts` that asserts `1 + 1 === 2` — this is the Wave 0 green baseline proving the test harness works. The real component tests are added in Plans 04 and 05.
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run --reporter=verbose 2>&1 | grep -E "PASS|pass|✓|Tests"</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run` exits 0 (all tests pass — at minimum the smoke test)
    - `grep -c 'jsdom' web/vitest.config.ts` returns >= 1 (jsdom environment configured)
    - `grep -c 'jest-dom' web/src/test/setup.ts` returns >= 1 (matchers imported)
    - `grep -c 'QueryClientProvider' web/src/main.tsx` returns >= 1
    - `grep -c 'RouterProvider' web/src/main.tsx` returns >= 1
    - `grep -c 'index.css' web/src/main.tsx` returns >= 1 (Tailwind styles loaded)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>Vitest with jsdom + jest-dom matchers configured; smoke test green; main.tsx entry with QueryClient + TanStack Router placeholder; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/node_modules | Third-party packages installed from npm; all packages verified via Package Legitimacy Audit in RESEARCH.md |
| web/ SPA build → browser | Static assets served by nginx; no secrets in the SPA bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | web/ SPA bundle (web/dist) | mitigate [BLOCKING] | flowd bearer token MUST NOT appear in the built SPA; the SPA never imports config secrets; grep gate: `grep -r 'Bearer\|FlowdToken\|flowd_token' web/src/` must return 0 lines; `grep -r 'Bearer' web/dist/` after build must return 0 |
| T-02-02 | Tampering | npm install — package version drift | mitigate | All packages pinned to exact versions in package.json with no range prefixes; TypeScript pinned to 5.9.3 explicitly to avoid 6.0.3 auto-install |
| T-02-03 | Information Disclosure | localStorage operator context | accept | Only non-secret tenant/user/project/session IDs stored in localStorage per D-07; operator token is in-memory only per D-01; acceptable for an internal operator tool |
| T-02-SC | Tampering | npm package install | mitigate | Package Legitimacy Audit in RESEARCH.md confirms all npm packages are legitimate; no [ASSUMED] or [SUS] packages; all approved from official repos |
</threat_model>

<verification>
## Wave 2 Phase Checks

1. `node -e "require('./web/package.json').devDependencies.typescript"` — returns `5.9.3`
2. `grep -c '@import "tailwindcss"' web/src/index.css` — returns 1
3. `test -f web/components.json` — exits 0
4. `test -d web/src/components/ui` — exits 0
5. `cd web && npx vitest run` — exits 0
6. `cd web && npx tsc --noEmit` — exits 0
7. `grep -rn 'Bearer\|FlowdToken\|flowd_token' web/src/` — returns 0 lines (no token references in SPA source)
8. `grep -c '8090' web/vite.config.ts` — returns >= 1 (dev proxy configured)
</verification>

<success_criteria>
- All npm packages installed at exact pinned versions
- TypeScript is 5.9.3, not 6.x
- Tailwind v4 via @tailwindcss/vite plugin; no tailwind.config.js
- index.css uses @import "tailwindcss" with dark-theme CSS variables from UI-SPEC
- shadcn initialized; Phase 1 UI components in src/components/ui/
- Vitest green (smoke test passes)
- main.tsx renders with React 19 + TanStack Router + QueryClientProvider
- No bearer token or BFF secrets in any web/src/ file
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
