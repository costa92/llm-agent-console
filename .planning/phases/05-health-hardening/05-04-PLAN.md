---
phase: 05-health-hardening
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - web/src/features/flow/FlowsPage.tsx
  - web/src/features/flow/RunDetailPage.tsx
  - web/src/features/chat/ChatPage.tsx
  - web/src/features/flow/components/TimelineView.tsx
  - web/src/features/flow/components/RunDetail.tsx
  - web/src/features/flow/FlowsPage.test.tsx
  - web/src/features/chat/ChatPage.reconnect.test.tsx
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "Every Memory/Flow/Chat surface conforms to the five-state contract (loading/empty/error/partial/ready) — no bare blank panel; the three grep-verified gap pages (FlowsPage, RunDetailPage, ChatPage) are confirmed or fixed (D-04, IC-5)"
    - "The flow timeline and chat stream views show a TRANSIENT reconnecting overlay ON TOP of the partial content: the partial timeline/trace stays visible, the badge flips to 'Reconnecting (n/N)…', and a muted subline 'Connection dropped — reconnecting… (attempt n of N)' appears (D-04, IC-4)"
    - "On cap exhaustion the overlay becomes the existing static-amber 'Connection lost' + Retry; the partial content stays (IC-4)"
    - "The four transport/result signals stay mutually distinct: green Streaming · amber-spinning Reconnecting(n/N) · amber-static Connection lost · red in-content Failed (IC-4)"
    - "All health/reconnect/state strings render as TEXT nodes — no dangerouslySetInnerHTML (T-V5)"
  artifacts:
    - path: "web/src/features/chat/ChatPage.tsx"
      provides: "reconnecting overlay (badge already wired; add the muted reconnecting subline) + confirmed five-state empty/streaming/error coverage"
      contains: "reconnecting"
    - path: "web/src/features/flow/components/TimelineView.tsx"
      provides: "reconnecting overlay subline beneath the partial timeline (partial stays visible) + attempt/cap forwarded into ConnectionBadge"
      contains: "reconnecting"
    - path: "web/src/features/flow/components/RunDetail.tsx"
      provides: "threads useRunStream's attempt/cap down into TimelineView (the badge's data source)"
      contains: "attempt"
    - path: "web/src/features/flow/FlowsPage.test.tsx"
      provides: "component test confirming FlowsPage renders loading/empty/error/ready (no blank panel)"
    - path: "web/src/features/chat/ChatPage.reconnect.test.tsx"
      provides: "component test: reconnecting overlay keeps partial visible; cap→Connection lost; signals distinct"
  key_links:
    - from: "web/src/features/flow/components/RunDetail.tsx"
      to: "web/src/features/flow/components/TimelineView.tsx"
      via: "destructure { attempt, cap } from useRunStream → pass as TimelineView props"
      pattern: "attempt"
    - from: "web/src/features/flow/components/TimelineView.tsx"
      to: "web/src/features/flow/components/ConnectionBadge.tsx"
      via: "forward attempt/cap into <ConnectionBadge attempt cap />"
      pattern: "ConnectionBadge"
    - from: "web/src/features/flow/components/TimelineView.tsx"
      to: "conn === 'reconnecting'"
      via: "transient overlay subline over the partial timeline"
      pattern: "reconnecting"
    - from: "web/src/features/chat/ChatPage.tsx"
      to: "conn === 'reconnecting'"
      via: "amber subline beneath the partial trace"
      pattern: "reconnecting"
---

<objective>
Slice B finish — the five-state audit (D-04) and the reconnecting OVERLAY on stream views.
Audit every Memory/Flow/Chat surface against the five-state contract and fix the three
grep-verified candidate gaps (FlowsPage / RunDetailPage / ChatPage), then add the transient
reconnecting overlay (badge + muted subline) ON TOP of the partial timeline/trace for the
flow timeline and chat — composing with, not replacing, the five states.

Purpose: no ambiguous blank screens anywhere, and a drop-in-progress reads instantly as
"re-connecting" — distinct from "gave up" and from "failed".
Output: audited/fixed gap pages + the reconnecting overlay + component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-health-hardening/05-RESEARCH.md
@.planning/phases/05-health-hardening/05-UI-SPEC.md
@.planning/phases/05-health-hardening/05-VALIDATION.md
@.planning/phases/05-health-hardening/05-03-SUMMARY.md

<read_first>
@web/src/features/flow/FlowsPage.tsx
@web/src/features/flow/RunDetailPage.tsx
@web/src/features/chat/ChatPage.tsx
@web/src/features/flow/components/TimelineView.tsx
@web/src/features/flow/components/RunDetail.tsx
@web/src/components/primitives/FiveStateWrapper.tsx
@web/src/features/flow/components/ConnectionBadge.tsx
</read_first>

<interfaces>
<!-- Audit findings (grep-verified) + the overlay contract. -->

Five-state audit (05-RESEARCH §Five-state audit, grep-verified):
  FlowsPage.tsx     — delegates to <FlowsTable /> which DOES use FiveStateWrapper (loading/empty/error/ready).
                      → CONFIRM conformance; add a component test proving the states render. Likely no code change.
  RunDetailPage.tsx — delegates to <RunDetail /> which DOES use FiveStateWrapper (loading/error/ready).
                      → CONFIRM conformance; fix only if a bare blank path is found.
  ChatPage.tsx      — does NOT use FiveStateWrapper; its empty (EmptyConversation), streaming
                      (Thinking…), error (in-bubble Failed), stopped, and dropped states are inline.
                      → CONFIRM each of the five states has a non-blank rendering; the inline coverage
                      is acceptable IF complete (the five-state contract is satisfied by equivalent
                      explicit states, not necessarily by the wrapper). Add the reconnecting subline.

ConnectionBadge ownership (grep-confirmed — the badge's render site + prop path):
  - <ConnectionBadge conn={conn} /> is rendered INSIDE TimelineView.tsx (TimelineView.tsx:178),
    NOT in RunDetail/RunDetailPage. RunDetailPage → RunDetail → TimelineView → ConnectionBadge.
  - useRunStream lives in RunDetail.tsx (RunDetailBody: `const stream = useRunStream()`), which today
    destructures only `const { timeline, conn } = stream` (RunDetail.tsx:111) and passes
    `<TimelineView timeline conn mode onRetry />` (RunDetail.tsx:199-207). attempt/cap are NOT threaded yet.
  - EXPLICIT prop path for { attempt, cap } from Plan-03's useRunStream to the badge:
      useRunStream() [RunDetail.tsx] --(destructure attempt, cap)-->
      <TimelineView attempt cap … /> [RunDetail.tsx:199] --(new TimelineViewProps fields)-->
      <ConnectionBadge conn attempt cap /> [TimelineView.tsx:178]
    → RunDetail.tsx IS on the threading path and IS in this plan's files_modified.

Overlay contract (05-UI-SPEC IC-4 — composes ON TOP of the five states, does not replace ready):
  - partial timeline/trace STAYS visible (already-arrived frames are `ready` content)
  - conn==='reconnecting' → ConnectionBadge shows "Reconnecting (n/N)…" (Plan 03 arm) + a muted Body
    subline: "Connection dropped — reconnecting… (attempt {n} of {N})."
  - conn==='errored' (cap exhausted) → existing static-amber "Connection lost" + the existing muted
    "Connection dropped before the run/reply finished — Retry" line (already present; keep)
  - terminal done/error ends normally (badge Closed; flow_err/chat error renders red in-content — a result)

From useRunStream / useChatStream (Plan 03) — the badge counter source:
  { conn, attempt, cap }   // attempt/cap feed ConnectionBadge's new (n/N) prop and the subline

From ConnectionBadge.tsx (Plan 03) — already accepts { conn, attempt?, cap? }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five-state audit — confirm/fix FlowsPage, RunDetailPage, ChatPage (+ FlowsPage test)</name>
  <files>web/src/features/flow/FlowsPage.tsx, web/src/features/flow/RunDetailPage.tsx, web/src/features/chat/ChatPage.tsx, web/src/features/flow/FlowsPage.test.tsx</files>
  <read_first>web/src/features/flow/FlowsPage.tsx, web/src/features/flow/RunDetailPage.tsx, web/src/features/chat/ChatPage.tsx, web/src/components/primitives/FiveStateWrapper.tsx</read_first>
  <action>Audit the three grep-verified candidate gap pages against the five-state contract (D-04 / IC-5 — a CODE conformance check, not a redesign). For FlowsPage and RunDetailPage: verify they delegate to children (FlowsTable / RunDetail) that already render loading/empty/error/ready via FiveStateWrapper — if so, no production change is required beyond confirming no bare-blank path; if a blank path exists (e.g. a child returns null with no state), wrap it. For ChatPage: confirm each of the five states has an explicit non-blank rendering (loading is N/A — chat has no initial fetch; empty = EmptyConversation; ready/partial = the transcript + Thinking… placeholder; error = in-bubble Failed; the dropped/errored marker). The inline coverage is acceptable IF complete — do NOT force FiveStateWrapper where explicit equivalent states already exist; only fix a genuine blank-screen gap. Render every string as a TEXT node (no dangerouslySetInnerHTML — T-V5). Add web/src/features/flow/FlowsPage.test.tsx (the audit evidence for the primary candidate): mock the flows query to drive loading → empty ("No flows yet.") → error → ready (rows), asserting a visible non-blank node in each state. Document the audit outcome (confirmed vs fixed, per page) in the SUMMARY.</action>
  <verify>
    <automated>cd web && npx vitest run src/features/flow/FlowsPage.test.tsx src/features/flow src/features/chat src/features/memory</automated>
  </verify>
  <done>FlowsPage/RunDetailPage/ChatPage confirmed conformant (or fixed) — no bare blank panel in any of the five states; FlowsPage.test.tsx proves loading/empty/error/ready; full memory/flow/chat suites green; audit outcome documented in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Reconnecting overlay on the flow timeline + chat (transient, over partial content)</name>
  <files>web/src/features/flow/components/TimelineView.tsx, web/src/features/flow/components/RunDetail.tsx, web/src/features/chat/ChatPage.tsx, web/src/features/chat/ChatPage.reconnect.test.tsx</files>
  <read_first>web/src/features/flow/components/TimelineView.tsx, web/src/features/flow/components/RunDetail.tsx, web/src/features/chat/ChatPage.tsx, web/src/features/flow/components/ConnectionBadge.tsx</read_first>
  <action>Add the transient reconnecting overlay (IC-4) to the two stream views, composing ON TOP of the partial content (never replacing `ready`). FLOW — the ConnectionBadge is rendered INSIDE TimelineView (TimelineView.tsx:178), so attempt/cap must be threaded through the EXPLICIT prop path useRunStream → RunDetail → TimelineView → ConnectionBadge: (a) in RunDetail.tsx, extend the destructure to `const { timeline, conn, attempt, cap } = stream` (RunDetail.tsx:111) and forward them on the existing `<TimelineView … />` (RunDetail.tsx:199) as new `attempt={attempt} cap={cap}` props; (b) in TimelineView.tsx, add `attempt?: number; cap?: number` to TimelineViewProps and forward them into `<ConnectionBadge conn={conn} attempt={attempt} cap={cap} />` (TimelineView.tsx:178) so it renders "Reconnecting (n/N)…" while conn==='reconnecting'; (c) beneath the partial timeline (which STAYS visible) add a muted Body subline when conn==='reconnecting': "Connection dropped — reconnecting… (attempt {attempt} of {cap})." Keep the existing conn==='errored' "Connection lost" + Retry treatment in TimelineView unchanged (cap-exhausted lands there from Plan 03). CHAT (ChatPage.tsx): the ConnectionBadge is already in the assistant bubble header — pass attempt/cap through (from useChatStream's { conn, attempt, cap }) so it shows "Reconnecting (n/N)…"; add a muted amber reconnecting subline beneath the partial trace when conn==='reconnecting' (NOTE per Plan 03 chat is manual-retry-only, so chat goes drop→errored immediately — the reconnecting subline is primarily exercised by flow, but wire chat's badge/subline for symmetry and in case of a brief reconnecting tick). Keep the existing droppedTransport (conn==='errored') "Connection dropped before the reply finished." line. Ensure the four signals stay mutually distinct (IC-4): green Streaming / amber-spinning Reconnecting(n/N) / amber-static Connection lost / red in-content Failed. All TEXT nodes (T-V5). Create web/src/features/chat/ChatPage.reconnect.test.tsx asserting: while reconnecting the partial trace stays visible + the amber subline shows the (n/N); on errored the static "Connection lost"/dropped line shows; the red in-content Failed (error frame) is visually separate from the amber transport states.</action>
  <verify>
    <automated>cd web && npx vitest run src/features/chat/ChatPage.reconnect.test.tsx src/features/flow</automated>
  </verify>
  <done>Flow + chat show a transient reconnecting overlay (badge (n/N) + muted subline) with the partial content kept; attempt/cap threaded useRunStream→RunDetail→TimelineView→ConnectionBadge; cap→"Connection lost"+Retry; the four signals stay distinct; ChatPage.reconnect.test.tsx green; tsc -b + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stream view ← reconnect/health state | rendered presentation of transport state; untrusted upstream-derived strings (flow_err/chat error) cross into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5 | Tampering (XSS) | five-state + overlay strings (incl. upstream flow_err/chat error text) | mitigate | All strings render as TEXT nodes; no dangerouslySetInnerHTML (inherited Phase 2-4 posture) — verified across the audited pages and the new overlay. |
| T-05-leak | Information Disclosure | (carried) /api/health DTO | mitigate | Owned by Plan 01; no health DTO surface in this plan — register completeness only. |
| T-05-storm | DoS (self-inflicted) | (carried) reconnect loop | mitigate | Owned by Plan 03 (capped backoff + stop-on-terminal); this plan only renders the resulting state. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (05-RESEARCH §Package Legitimacy Audit: N/A). |

No new trust boundary is introduced — this plan is pure presentation conformance + a transient overlay.
</threat_model>

<verification>
- `cd web && npx vitest run src/features` green (five-state audit + reconnect overlay across memory/flow/chat).
- `cd web && npx vitest run` full TS suite green; `GOWORK=off go test ./...` green (phase gate).
- `cd web && npx tsc -b` clean; `cd web && npm run lint` clean.
- Manual (deferred, 05-VALIDATION Manual-Only): induce a live flow drop → partial timeline stays + "Reconnecting (n/N)…" subline → resume; cap → "Connection lost" + Retry.
</verification>

<success_criteria>
- D-04 / IC-5: every Memory/Flow/Chat surface conforms to the five-state contract; the three gap pages confirmed or fixed with audit evidence (FlowsPage test).
- IC-4: the reconnecting overlay is transient and composes ON TOP of the partial content (never replaces ready); cap→Connection lost+Retry keeps the partial.
- attempt/cap reach the badge via the pinned path useRunStream→RunDetail→TimelineView→ConnectionBadge (flow) and useChatStream→ChatPage→ConnectionBadge (chat).
- The four transport/result signals stay mutually distinct (operator-critical).
- All strings are TEXT nodes (T-V5).
</success_criteria>

<output>
Create `.planning/phases/05-health-hardening/05-04-SUMMARY.md` when done.
</output>
