---
phase: 05-health-hardening
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - web/src/features/flow/timeline/useRunStream.ts
  - web/src/features/flow/timeline/useRunStream.reconnect.test.ts
  - web/src/features/chat/turns/useChatStream.ts
  - web/src/features/chat/turns/useChatStream.reconnect.test.ts
  - web/src/features/flow/components/ConnectionBadge.tsx
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "On a FLOW transport drop, the hook auto-reconnects with capped backoff: conn shows 'reconnecting' with an (n/N) attempt counter, resumes via the /events hydrate+de-dup retry() seam, and stops on terminal (D-03, IC-3)"
    - "Flow reconnect resume preserves the (kind,node,ordinal) de-dup invariant — no lost or duplicated events on resume (D-03)"
    - "Flow cap exhaustion lands 'errored' ('Connection lost') via reconnect-give-up (D-03, Pitfall 1)"
    - "CHAT is MANUAL-RETRY-ONLY on a transport drop — it does NOT auto-restart the stream (refines CONTEXT D-03's 'chat re-opens the stream'): a chat drop goes straight to 'errored' so the operator hits Retry, because chat has no de-dup seam and an auto re-open would duplicate the whole answer (resolves Open-Question #1)"
    - "A content error frame (flow_err / chat error) is a terminal → 'closed' + red in-content, NEVER reconnecting (D-03)"
    - "The reconnect timer is cleared on terminal, success, unmount, reset, and Stop — no timers fire after the stream ends (Pitfall 4)"
    - "ConnectionBadge gains a 'reconnecting' arm: amber + spinning Loader + 'Reconnecting (n/N)…'; distinct from static-amber 'Connection lost' (UI-SPEC IC-2)"
  artifacts:
    - path: "web/src/features/flow/timeline/useRunStream.ts"
      provides: "auto-reconnect loop driving retry() on a backoff timer + attempt/cap exposed"
      contains: "nextDelay"
    - path: "web/src/features/chat/turns/useChatStream.ts"
      provides: "manual-retry-only drop handling (transport-error → reconnect-give-up immediately) + a retry() re-open"
      contains: "reconnect-give-up"
    - path: "web/src/features/flow/components/ConnectionBadge.tsx"
      provides: "reconnecting arm (amber spinner + (n/N) counter)"
      contains: "reconnecting"
    - path: "web/src/features/flow/timeline/useRunStream.reconnect.test.ts"
      provides: "drop→reconnecting(n/N)→resume-via-/events-dedup→terminal + cap→errored, via the fake SSE emitter"
    - path: "web/src/features/chat/turns/useChatStream.reconnect.test.ts"
      provides: "chat drop → errored (no auto re-open) + manual retry() re-opens"
  key_links:
    - from: "web/src/features/flow/timeline/useRunStream.ts"
      to: "retry() / listRunEvents"
      via: "setTimeout(nextDelay(attempt)) → retry() on fire"
      pattern: "nextDelay"
    - from: "web/src/features/flow/components/ConnectionBadge.tsx"
      to: "ConnState 'reconnecting'"
      via: "STATE_META reconnecting arm + (n/N) counter prop"
      pattern: "Reconnecting"
---

<objective>
Slice B wiring — turn the extended machine (Plan 02) into live recovery. Drive the
auto-reconnect loop into `useRunStream` (FLOW: capped-backoff timer → the existing
`retry()` `/events`-hydrate+de-dup seam → resume → stop-on-terminal → cap→errored), apply
the CHAT manual-retry-only policy into `useChatStream` (a documented refinement of CONTEXT
D-03), and arm `ConnectionBadge` with the `reconnecting` state (amber spinner + (n/N)). The
reconnect layer is fully unit-tested with the reused fake SSE emitter — no live backend.

Purpose: drops become recoverable for flow without duplication, and chat avoids re-streaming
a duplicated answer — the operator-critical distinction between "re-connecting", "gave up",
and "failed".
Output: reconnect-driven `useRunStream`/`useChatStream` + reconnect tests + the badge arm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-health-hardening/05-RESEARCH.md
@.planning/phases/05-health-hardening/05-UI-SPEC.md
@.planning/phases/05-health-hardening/05-VALIDATION.md
@.planning/phases/05-health-hardening/05-02-SUMMARY.md

<read_first>
@web/src/features/flow/timeline/useRunStream.ts
@web/src/features/chat/turns/useChatStream.ts
@web/src/features/flow/components/ConnectionBadge.tsx
@web/src/test/mocks/fetch-event-source.ts
@web/src/features/flow/timeline/connection.ts
@web/src/features/flow/timeline/backoff.ts
</read_first>

<interfaces>
<!-- Contracts the executor uses directly (Plan 02 output + executed hooks). -->

From Plan 02 — connection.ts (extended) + backoff.ts:
  type ConnState = 'idle' | 'streaming' | 'reconnecting' | 'closed' | 'errored'
  ConnEvent adds: { type:'reconnect-success' } | { type:'reconnect-give-up' }
  nextDelay(attempt, opts=DEFAULT_BACKOFF, rng=Math.random): number   // full-jitter, pure
  DEFAULT_BACKOFF = { baseMs:1000, factor:2, maxMs:30000, cap:5 }

From useRunStream.ts (executed) — the FLOW reconnect seam already exists:
  retry(): Promise<void>
    // if runId known → listRunEvents(runId) folded as source:'history'; reducer de-dups
    //                  (kind,node,ordinal) → lossless, duplicate-free resume
    // if runId NOT known → re-open runStream (a genuinely new attempt — no run created yet)
  onError today: () => dispatchConn({ type:'transport-error' })   // CHANGE: now → reconnecting; drive the loop
  abortRef / runIdRef / unmount-abort already present

From useChatStream.ts (executed) — chat has NO de-dup/replay (Phase-4 contract):
  send(message), sendSync(message), stop(), newSession()
  onError live-drop today: dispatchConn({ type:'transport-error' })  // CHANGE per refinement below
  ChatPage.tsx reads `conn === 'errored'` for the dropped-transport marker (must still be reached)

From ConnectionBadge.tsx (executed) — STATE_META has 3 arms (streaming/closed/errored):
  reconnecting arm to add: token 'var(--status-degraded)' (AMBER), label 'Reconnecting', Icon Loader (animate-spin)
  spinner ternary currently: conn === 'streaming' ? animate-spin → extend to include 'reconnecting'
  (n/N) counter is a NEW optional prop (attempt, cap) — ConnState alone can't carry it

From web/src/test/mocks/fetch-event-source.ts (executed) — drop+resume support:
  makeFakeSseStream(): { openSseStream, emitOpen(headers), emit(frames), close(), fail(err) }
  goldenSuccess (flow_started→node_started→node_finished→flow_done)
  // mock '@/lib/sse' with fake.openSseStream; mock listRunEvents for the flow /events hydrate
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FLOW auto-reconnect loop in useRunStream (capped backoff → retry() → resume/give-up)</name>
  <files>web/src/features/flow/timeline/useRunStream.ts, web/src/features/flow/timeline/useRunStream.reconnect.test.ts</files>
  <read_first>web/src/features/flow/timeline/useRunStream.ts, web/src/test/mocks/fetch-event-source.ts, web/src/features/flow/timeline/backoff.ts</read_first>
  <behavior>
    - open, emit a prefix (flow_started, node_started), then fake.fail() (transport drop) → conn === 'reconnecting' and the exposed attempt counter starts at 1
    - the loop fires retry() on a timer (use vi.useFakeTimers + advance by nextDelay): with runId known, retry() calls listRunEvents(runId) (mocked to return the prefix + tail); the reducer de-dups (kind,node,ordinal) → the timeline holds each event exactly ONCE (no duplication of the prefix)
    - after a successful resume the conn returns to 'streaming' (reconnect-success); emitting the terminal flow_done → conn 'closed' and the loop stops (no further timer fires)
    - cap path: if every retry() attempt keeps failing, after N=5 attempts conn → 'errored' via reconnect-give-up ('Connection lost')
    - terminal-wins: a flow_done arriving mid-reconnect settles 'closed' and clears the timer (no storm)
    - no timer fires after unmount / after terminal (assert no pending timers / no post-terminal listRunEvents calls)
  </behavior>
  <action>Extend useRunStream.ts to own the reconnect loop. Add an attemptRef (number) and a timerRef (ReturnType<typeof setTimeout> | null) and expose { attempt, cap } (cap = DEFAULT_BACKOFF.cap) on the returned object so the badge can show (n/N). Change the live onError handler: instead of dispatchConn({type:'transport-error'}) landing immediately errored, it now starts the reconnect loop — dispatchConn({type:'transport-error'}) (→ 'reconnecting' via Plan 02), set attemptRef=1, and scheduleReconnect(). scheduleReconnect(): if attemptRef > DEFAULT_BACKOFF.cap → dispatchConn({type:'reconnect-give-up'}) (→ 'errored') and stop; else timerRef = setTimeout(async () => { await retry(); }, nextDelay(attemptRef-1)) — on the retry resolving WITHOUT a terminal, if the stream did not resume, increment attemptRef and scheduleReconnect() again; on a successful resume (a frame arrives / retry opens) dispatchConn({type:'reconnect-success'}) (→ 'streaming') and reset attemptRef. Reuse the EXISTING retry() seam verbatim for the hydrate+de-dup (do NOT re-POST /run/stream for a created run). Preserve terminal-wins: onFrame's existing terminal branch must also clearReconnect() (clear timerRef, reset attemptRef). Add clearReconnect() and call it on terminal, reconnect-success, reset, and in the unmount effect alongside the existing abort (Pitfall 4). Manual retry() stays callable (it already exists) and resets attemptRef + fires immediately. Create useRunStream.reconnect.test.ts using makeFakeSseStream + vi.mock('@/lib/sse') + vi.mock the flow client's listRunEvents, with vi.useFakeTimers to advance the backoff deterministically (or inject rng=()=>0 so nextDelay≈0). Cover the behaviors above, especially the de-dup-on-resume invariant and the cap→errored path.</action>
  <verify>
    <automated>cd web && npx vitest run src/features/flow/timeline/useRunStream.reconnect.test.ts</automated>
  </verify>
  <done>Flow drop → reconnecting(1/N) → resume-via-/events with de-dup (each event once) → terminal closed; cap→errored; terminal-wins clears the timer; no post-terminal/post-unmount timer fires. Full flow timeline suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CHAT manual-retry-only drop policy (D-03 refinement) + ConnectionBadge reconnecting arm</name>
  <files>web/src/features/chat/turns/useChatStream.ts, web/src/features/chat/turns/useChatStream.reconnect.test.ts, web/src/features/flow/components/ConnectionBadge.tsx</files>
  <read_first>web/src/features/chat/turns/useChatStream.ts, web/src/features/flow/components/ConnectionBadge.tsx, web/src/features/chat/ChatPage.tsx</read_first>
  <behavior>
    - chat live drop: a streamed turn opens (openedRef true), emits a step, then fake.fail() → conn === 'errored' (NOT 'reconnecting'): chat does NOT auto-restart the stream (no second openSseStream call) — the answer is never re-streamed/duplicated
    - a manual retry() on chat re-opens the stream (a fresh openSseStream call) from the dropped 'errored' state — operator-driven only
    - Stop still settles 'closed' (unchanged); a non-2xx open is still a send-failure (onSendError), NOT errored (unchanged)
    - ConnectionBadge: conn='reconnecting' renders amber (var(--status-degraded)) with a spinning Loader and label text containing the (n/N) counter (e.g. "Reconnecting (2/5)…"); conn='errored' still renders static Unplug "Connection lost" (no spinner) — the two amber states stay visually distinct
  </behavior>
  <action>In useChatStream.ts apply the documented D-03 REFINEMENT (resolves Open-Question #1): chat has no seq/ordinals and no de-dup seam (Phase-4 contract), so an auto re-open would re-stream the whole answer duplicated. Therefore CHAT is manual-retry-only on a transport drop. Concretely: on a live drop (openedRef true, not ended), dispatch the machine straight to errored via dispatchConn({type:'transport-error'}) THEN dispatchConn({type:'reconnect-give-up'}) so the now-reconnecting-capable machine still reaches 'errored' immediately (Pitfall 1 — do NOT strand chat in 'reconnecting'). Keep the existing onError/.catch error-channel split (non-2xx open → onSendError unchanged; ended/stopped guard unchanged). Add a retry() to the hook (and to UseChatStream): re-open chatStream(lastMessageRef, sessionRef.current, …) as a fresh attempt (operator-driven). Document inline that this REFINES CONTEXT D-03's "chat re-opens the stream" — the research revealed the duplication problem, so flow auto-reconnects (de-dups) while chat is manual-retry-only; both still stop on terminal and both reach 'errored' on a drop/cap. In ConnectionBadge.tsx add the reconnecting arm to STATE_META { token:'var(--status-degraded)', label:'Reconnecting', Icon: Loader }, extend the spinner ternary to spin for 'reconnecting' too, and add an optional { attempt?: number; cap?: number } prop so the label renders "Reconnecting ({attempt}/{cap})…" when reconnecting (bare "Reconnecting…" if counts absent — UI-SPEC NBR-3). Keep the existing three arms untouched. Create useChatStream.reconnect.test.ts (fake emitter) covering: drop→errored (single openSseStream call, no auto re-open), manual retry()→a second openSseStream call, Stop→closed unchanged, non-2xx→send-failure unchanged.</action>
  <verify>
    <automated>cd web && npx vitest run src/features/chat/turns/useChatStream.reconnect.test.ts</automated>
  </verify>
  <done>Chat drop → errored with NO auto re-open (no duplication); manual retry() re-opens; Stop/send-failure unchanged; ConnectionBadge reconnecting arm = amber spinner + (n/N), distinct from static "Connection lost"; tsc -b + existing chat/flow suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SSE client → BFF stream proxy | the reconnect loop re-drives the existing authed stream hop; inherits each stream's auth posture (flow: BFF injects flowd bearer; chat: none) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-storm | Denial of Service (self-inflicted) | reconnect loop hammering an upstream after a real failure | mitigate | Capped backoff (N=5) + full-jitter + stop-on-terminal (D-03); the loop clears on terminal/unmount/reset/Stop (Pitfall 4). Chat does not auto-loop at all (manual-retry-only). |
| T-05-leak2 | Information Disclosure | reconnect/error strings rendered in ConnectionBadge / overlays | mitigate | All strings render as TEXT nodes (inherited Phase 2-4); no dangerouslySetInnerHTML; no upstream URL/error in any badge label. |
| T-05-dup | Integrity (UX) | chat answer duplication on auto re-open | mitigate | Chat is manual-retry-only (D-03 refinement) — no auto re-stream; flow auto-reconnects only because it de-dups on (kind,node,ordinal). |
| T-05-leak | Information Disclosure | (carried) /api/health DTO | mitigate | Owned by Plan 01 — referenced here for register completeness; no health surface in this plan. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO new packages (05-RESEARCH §Package Legitimacy Audit: N/A); no slopcheck needed. |

Auth posture: reconnect introduces no new auth surface (V3 no) — it re-drives the existing stream's authed request.
</threat_model>

<verification>
- `cd web && npx vitest run src/features/flow/timeline src/features/chat` green (reconnect resume+dedup, cap→errored, chat manual-retry-only, badge arm).
- `cd web && npx tsc -b` clean — the widened ConnState + new badge prop compile across all callers.
- `cd web && npm run lint` clean for touched files.
- Manual (deferred to live stack, 05-VALIDATION Manual-Only): induce a mid-run flow drop → "Reconnecting (n/N)…" then lossless resume via /events; exhaust cap → "Connection lost" + Retry.
</verification>

<success_criteria>
- D-03 flow: auto-reconnect with capped backoff, de-dup-preserving resume, stop-on-terminal, cap→errored — all automated via the fake emitter.
- D-03 refinement (Open-Question #1 resolved): chat is manual-retry-only on a drop, documented inline vs CONTEXT D-03, proven to NOT auto-re-open (no duplication).
- Pitfall 1 honored: neither hook strands in 'reconnecting' — chat reaches 'errored' immediately, flow reaches it at cap.
- Pitfall 4 honored: timers cleared on terminal/success/unmount/reset/Stop.
- ConnectionBadge reconnecting arm distinct from "Connection lost".
</success_criteria>

<output>
Create `.planning/phases/05-health-hardening/05-03-SUMMARY.md` when done.
</output>
