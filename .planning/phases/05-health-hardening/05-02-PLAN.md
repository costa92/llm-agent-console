---
phase: 05-health-hardening
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/src/features/flow/timeline/connection.ts
  - web/src/features/flow/timeline/connection.test.ts
  - web/src/features/flow/timeline/backoff.ts
  - web/src/features/flow/timeline/backoff.test.ts
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "The connection machine gains a 'reconnecting' state purely additively — every existing caller still compiles and behaves identically until the loop is wired (D-03)"
    - "On a transport drop while streaming, the machine enters 'reconnecting' (not immediate 'errored') (D-03, Pitfall 1)"
    - "A terminal done/error ALWAYS wins — even mid-reconnect it settles 'closed' (no reconnect storm) (D-03)"
    - "Cap exhaustion (reconnect-give-up) is the ONLY path from 'reconnecting' to 'errored' (D-03, Pitfall 1)"
    - "nextDelay(attempt) is a PURE capped-exponential-backoff-with-jitter function with an injected rng — deterministic in tests (D-03)"
  artifacts:
    - path: "web/src/features/flow/timeline/connection.ts"
      provides: "ConnState union + 'reconnecting'; connReducer with reconnect-success/reconnect-give-up + terminal-wins guard"
      contains: "reconnecting"
    - path: "web/src/features/flow/timeline/backoff.ts"
      provides: "pure nextDelay(attempt, opts, rng) full-jitter scheduler + BackoffOpts + DEFAULT_BACKOFF"
      exports: ["nextDelay", "DEFAULT_BACKOFF"]
    - path: "web/src/features/flow/timeline/connection.test.ts"
      provides: "reconnecting-transition tests extending the existing suite"
    - path: "web/src/features/flow/timeline/backoff.test.ts"
      provides: "deterministic sequence/cap/jitter-bound/reset tests with injected rng"
  key_links:
    - from: "web/src/features/flow/timeline/connection.ts"
      to: "connReducer"
      via: "reconnect-success → streaming; reconnect-give-up → errored; terminal → closed"
      pattern: "reconnect-give-up"
---

<objective>
Slice B foundation — the pure, isolated reconnect primitives. Extend the Phase-3/4
`connection.ts` machine ADDITIVELY with a `reconnecting` state (exactly as its doc-comment
anticipates), and add a pure `backoff.ts` `nextDelay()` capped-exponential-backoff-with-jitter
scheduler with an injected rng. Both are unit-tested in full isolation — no hooks, no timers,
no live backend.

Purpose: keep the load-bearing reconnect logic (transition guards + backoff schedule)
deterministic and provable BEFORE any timer-driven wiring (Plan 03). This is a dedicated TDD
plan because the transition guards (Pitfall 1: terminal-wins, give-up-is-the-only-errored-path)
are exactly the kind of defined-I/O logic that must be RED→GREEN proven.
Output: extended `connection.ts` + `backoff.ts` + their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-health-hardening/05-RESEARCH.md
@.planning/phases/05-health-hardening/05-UI-SPEC.md
@.planning/phases/05-health-hardening/05-VALIDATION.md

<read_first>
@web/src/features/flow/timeline/connection.ts
@web/src/features/flow/timeline/connection.test.ts
</read_first>

<interfaces>
<!-- The CURRENT contract (executed) the executor extends ADDITIVELY. -->

From web/src/features/flow/timeline/connection.ts (executed — every caller consumes ConnState):
  type ConnState = 'idle' | 'streaming' | 'closed' | 'errored'
  type ConnEvent = { type:'start' } | { type:'terminal' } | { type:'transport-error' } | { type:'reset' }
  const initialConn: ConnState = 'idle'
  function connReducer(state, event): ConnState
    // current guards: terminal→closed; transport-error→ (state==='closed' ? 'closed' : 'errored')

From connection.test.ts (executed) — the run() reducer-fold helper to reuse:
  function run(events: ConnEvent[], from = initialConn): ConnState

The TARGET extension shape (05-RESEARCH Pattern 3 — the contract, not final code):
  type ConnState = 'idle' | 'streaming' | 'reconnecting' | 'closed' | 'errored'
  type ConnEvent = …existing… | { type:'reconnect-success' } | { type:'reconnect-give-up' }
  Guards:
    terminal      from ANY non-idle  → 'closed'   (wins even mid-reconnect — D-03 no storms)
    transport-error while 'closed'   → 'closed'   (existing terminal-then-error guard, KEEP)
    transport-error while 'streaming'→ 'reconnecting'   (was → 'errored')
    transport-error while 'reconnecting' → 'reconnecting'  (idempotent; the loop owns give-up)
    reconnect-success                → 'streaming'
    reconnect-give-up                → 'errored'

Backoff numbers (05-UI-SPEC IC-2 / Pattern 4, planner-locked; cap is fixed):
  base 1000ms, factor 2, maxDelay 30000ms, cap N = 5, full-jitter
  full jitter: delay = floor(rng() * min(maxMs, baseMs * factor**attempt))
  Convention (Pitfall 5): internal attempt index 0..N-1 into nextDelay; the badge shows 1..N.
</interfaces>
</context>

<feature>
  <name>connection.ts reconnecting extension + pure backoff scheduler</name>
  <files>web/src/features/flow/timeline/connection.ts, web/src/features/flow/timeline/connection.test.ts, web/src/features/flow/timeline/backoff.ts, web/src/features/flow/timeline/backoff.test.ts</files>
  <behavior>
    connReducer (extend connection.test.ts with new it() cases, reusing run()):
    - 'start' → 'streaming' (unchanged)
    - existing: a transport-error after terminal stays 'closed' (KEEP — must still pass)
    - drop → reconnecting → success → streaming: run(['start','transport-error','reconnect-success']) === 'streaming'
    - drop → cap exhausted → errored: run(['start','transport-error','reconnect-give-up']) === 'errored'
    - terminal wins mid-reconnect (no storm): run(['start','transport-error','terminal']) === 'closed'
    - transport-error while reconnecting is idempotent: run(['start','transport-error','transport-error']) === 'reconnecting'
    - reconnect-give-up only reaches errored from reconnecting (NOT a fresh transport-error) — proves Pitfall 1: transport-error alone never lands errored anymore
    backoff.nextDelay (backoff.test.ts, injected rng):
    - rng=()=>0 → delay 0 at every attempt (lower bound)
    - rng=()=>0.999 → delay ≈ ceiling-1 at each attempt; attempt 0 ceiling=base, attempt large clamps to maxMs
    - ceiling grows base·2^attempt until clamped at maxMs (assert attempt where base·2^attempt > maxMs returns ≤ maxMs)
    - delay is always an integer in [0, ceiling)
    - DEFAULT_BACKOFF exports { baseMs:1000, factor:2, maxMs:30000, cap:5 }
  </behavior>
  <implementation>RED first: add the new it() cases to connection.test.ts (they fail against the current 4-state union) and create backoff.test.ts with the injected-rng cases (fails — no module). GREEN: extend connection.ts — add 'reconnecting' to ConnState, add { type:'reconnect-success' } and { type:'reconnect-give-up' } to ConnEvent, and in connReducer add cases: 'reconnect-success' → 'streaming', 'reconnect-give-up' → 'errored', and change 'transport-error' so that state==='streaming' → 'reconnecting' (state==='closed' stays 'closed'; state==='reconnecting' stays 'reconnecting'; otherwise current behavior). Preserve 'terminal' → 'closed' as the unconditional winner (it already is). Update ONLY the connection.ts doc-comment to note Phase 5 added 'reconnecting' — do not touch behavior of other events. Create backoff.ts exporting type BackoffOpts = { baseMs; factor; maxMs; cap }, DEFAULT_BACKOFF, and nextDelay(attempt, o=DEFAULT_BACKOFF, rng=Math.random) returning Math.floor(rng() * Math.min(o.maxMs, o.baseMs * o.factor ** attempt)) — pure, no Date.now, no timers (05-RESEARCH Pattern 4). REFACTOR only if needed. Do NOT wire any hook here (Plan 03 owns the loop).</implementation>
</feature>

<verification>
- `cd web && npx vitest run src/features/flow/timeline/connection.test.ts src/features/flow/timeline/backoff.test.ts` green (RED→GREEN demonstrated in commits).
- Full timeline suite regression: `cd web && npx vitest run src/features/flow/timeline` green — every existing connection/reducer/useRunStream test still passes (additive change, no caller breakage).
- `cd web && npx tsc -b` clean — all existing ConnState consumers (ConnectionBadge, useRunStream, useChatStream, ChatPage, RunDetail) still compile against the widened union.
</verification>

<success_criteria>
- D-03 machine contract proven: drop→reconnecting, success→streaming, give-up→errored, terminal-wins-mid-reconnect→closed.
- Pitfall 1 proven: transport-error no longer lands directly in errored; only reconnect-give-up does.
- nextDelay is pure + deterministic under injected rng; cap and clamp tested.
- Zero existing-test regressions (purely additive extension).
</success_criteria>

<output>
Create `.planning/phases/05-health-hardening/05-02-SUMMARY.md` when done.
</output>
