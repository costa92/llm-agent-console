---
phase: 03-flow-console
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - web/src/features/flow/components/TimelineView.tsx
  - web/src/features/flow/components/TimelineView.test.tsx
  - web/src/features/flow/components/NodeStatusList.tsx
  - web/src/features/flow/components/ConnectionBadge.tsx
  - web/src/features/flow/components/RunTrigger.tsx
  - web/src/features/flow/components/RunResultPanel.tsx
  - web/src/features/flow/hooks/useSyncRun.ts
  - web/src/features/flow/FlowDetailPage.tsx
autonomous: true
requirements: [FLOW-03, FLOW-04]
must_haves:
  truths:
    - "The TimelineView (live append-only event timeline) + NodeStatusList + ConnectionBadge + RunResultPanel components are built here and render the Plan-03 reducer model (they are MOUNTED at the run sub-route in Plan 05 — NOT in the Definition tab) (D-01 timeline + node-status strip)"
    - "A per-node status strip updates IN PLACE (pending→running→done/skipped/errored) fed by the same reducer (D-01 node-status strip)"
    - "A connection-state badge shows streaming / closed / errored in the timeline header (D-02 connection-state badge)"
    - "Auto-scroll follows the newest frame but PAUSES on manual scroll-up; a Jump-to-latest pill resumes following (D-03 auto-scroll pauses on manual scroll + jump-to-latest)"
    - "A flow_err renders as a RED terminal frame IN the timeline body; a transport drop flips the AMBER 'Connection lost' badge with a manual Retry that calls the Plan-03 hook's retry() (which hydrates GET /runs/{id}/events + de-dups for the known runId, NOT a fresh /run/stream) — the two are never confused (D-09 / IC-6)"
    - "The Definition-tab Run trigger starts the streamed run and, on X-Run-ID, NAVIGATES to /flows/{id}/runs/{runId} (the single live+replay render location) — the live timeline is NOT mounted in the Definition tab (D-08)"
    - "Operator triggers a synchronous run (secondary Run sync) and its {outputs} render into the RunResultPanel result surface (D-04 streamed primary + sync secondary, ONE result surface)"
  artifacts:
    - path: "web/src/features/flow/components/TimelineView.tsx"
      provides: "renders the reducer model: append-only frames + per-frame status icon/color + collapsed raw-JSON + auto-scroll-pause + connection badge + onRetry hook for D-09"
      min_lines: 50
    - path: "web/src/features/flow/components/RunTrigger.tsx"
      provides: "inputs form + Run (streamed → navigates to sub-route on X-Run-ID) / Run (sync) → result surface"
    - path: "web/src/features/flow/components/ConnectionBadge.tsx"
      provides: "streaming/closed/errored badge per UI-SPEC color table"
  key_links:
    - from: "web/src/features/flow/components/RunTrigger.tsx"
      to: "web/src/features/flow/timeline/useRunStream.ts"
      via: "start(flowId, inputs, { onRunId }) → on X-Run-ID navigate to /flows/{id}/runs/{runId}"
      pattern: "useRunStream|start\\(|onRunId|runs/"
    - from: "web/src/features/flow/components/TimelineView.tsx"
      to: "web/src/features/flow/timeline/reducer.ts model"
      via: "renders timeline.events + nodeStatus + terminal; onRetry → hook.retry()"
      pattern: "timeline\\.events|nodeStatus|onRetry"
    - from: "web/src/features/flow/hooks/useSyncRun.ts"
      to: "/api/flow/flows/{id}/run"
      via: "runSync mutation → RunResultPanel"
      pattern: "runSync"
---

<objective>
Slice B (the keystone, UI half) — BUILD the live-run render components and wire the streamed-run trigger to the deep-linkable sub-route. Build `TimelineView` (append-only frames with per-frame-kind icons/colors, collapsed per-frame raw JSON, auto-scroll-that-pauses, the connection-state badge, and an `onRetry` affordance), a `NodeStatusList` strip, a `ConnectionBadge`, a `RunResultPanel`, and the `RunTrigger` (Run streamed primary + Run sync secondary). The operator-critical D-09 distinction (red `flow_err` in-body vs amber 'Connection lost' in-header) lives in TimelineView, and its Retry calls the Plan-03 hook's `retry()` which hydrates `GET /runs/{id}/events` for the known runId (IC-6 — re-POSTing /run/stream would start a NEW run). The streamed Run trigger starts the run and, on `X-Run-ID`, NAVIGATES to `/flows/{id}/runs/{runId}` — the live timeline renders there (Plan 05), NOT in the Definition tab. This is where SSE-through-BFF is proven end-to-end against real flowd (manual gate), closing Phase-1's deferred BFF-03 Part 2.

Purpose: success criteria 2 + 3 — sync outputs and the live append-only timeline with per-node status, auto-scroll-pause, and visible connection state; plus the D-08 single-render-location restructure (live + replay both render at the sub-route).
Output: the timeline + run-trigger + result components; the Definition tab hosts the RunTrigger (which navigates), the sync RunResultPanel, and the run-history slot — the live TimelineView is mounted at the run sub-route in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flow-console/03-RESEARCH.md
@.planning/phases/03-flow-console/03-UI-SPEC.md
@.planning/phases/03-flow-console/03-VALIDATION.md
@.planning/phases/03-flow-console/03-02-SUMMARY.md
@.planning/phases/03-flow-console/03-03-SUMMARY.md

@web/src/components/primitives/RawJsonViewer.tsx
@web/src/components/primitives/FiveStateWrapper.tsx

<interfaces>
<!-- From 03-03 (consume directly):
  useRunStream(): { timeline, conn, runId, start(flowId,inputs,opts?), replay(runId), retry } where
    timeline = { events: TimelineEvent[], nodeStatus: Record<string,NodeStatus>, terminal: 'none'|'done'|'error', outputs?, error? }
    conn: 'idle'|'streaming'|'closed'|'errored'
    runId: the live run id once flowd's X-Run-ID arrives (D-08)
    start(flowId, inputs, { onRunId }) — onRunId(runId) fires when X-Run-ID arrives
    retry() — on a known runId, hydrates GET /runs/{id}/events + de-dups (D-09 / IC-6); does NOT re-POST /run/stream for an already-created run
  TimelineEvent = { kind: SseKind; node?: string; payload: SsePayload; ordinal; source }
From 03-01: runSync(flowId, inputs) → {outputs, run_id}; parseFlowdError.
From 03-02: FlowDetailPage Definition tab exposes a run-trigger slot + a run-history slot (Slice C). -->
</interfaces>

<ui_spec_refs>
<!-- 03-UI-SPEC.md governs every visual here. Load-bearing tables:
- Color (a) per-frame-kind icon+color (flow_started slate, node_started slate/loader, node_finished green, node_skipped muted, flow_done green, flow_err RED)
- Color (b) per-node status strip (pending/running/done/skipped/errored)
- Color (c) connection badge (streaming GREEN, closed neutral, errored AMBER "Connection lost")
- IC-3 timeline + node strip + auto-scroll-pause; IC-4 one result surface; IC-5 terminal close; IC-6 D-09 distinction (red flow_err in-body vs amber transport-drop in-header; transport-drop Retry recovers via /events hydrate, NOT a new run); IC-7/S8 the run sub-route is the SINGLE live+replay render location.
- Copywriting: "Run" (primary accent), "Run (sync)" (neutral), "Retry", "Jump to latest", flow_err frame "Flow failed — {error}.", connection-lost copy, empty/idle timeline copy.
- jsdom has no layout: stub scrollTo (Phase-1 precedent); assert auto-scroll INTENT (scroll-to-bottom called when not paused, skipped when paused), not pixels. -->
</ui_spec_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimelineView + NodeStatusList + ConnectionBadge (per-frame colors, raw-JSON, D-09 rendering + /events-hydrate Retry)</name>
  <files>web/src/features/flow/components/TimelineView.tsx, web/src/features/flow/components/NodeStatusList.tsx, web/src/features/flow/components/ConnectionBadge.tsx, web/src/features/flow/components/TimelineView.test.tsx</files>
  <read_first>
    - web/src/components/primitives/RawJsonViewer.tsx (collapsed-by-default per-frame payload viewer)
    - 03-UI-SPEC.md Color tables (a)/(b)/(c) + IC-3 + IC-6 (the D-09 red-in-body vs amber-in-header contract; transport-drop Retry recovers via /events hydrate, not a new run)
    - 03-VALIDATION.md "Auto-scroll-pause" testing note (stub scrollTo; assert intent)
    - 03-03-SUMMARY.md (the timeline render model + conn union shapes + retry() semantics)
  </read_first>
  <action>Build `ConnectionBadge.tsx` mapping ConnState → the UI-SPEC (c) color/icon/label (streaming GREEN loader/"Streaming"; closed neutral "Closed"; errored AMBER unplug/"Connection lost"). Build `NodeStatusList.tsx` rendering the per-node status strip from `timeline.nodeStatus` with the (b) color table (pending/running/done/skipped muted-0.6/errored red), each chip color+icon+text (never color alone). Build `TimelineView.tsx`: an append-only scroll-area list over `timeline.events` — each row = gutter status icon+color per the (a) per-frame-kind table + frame-kind label (sans Body) + mono node name (when present) + a COLLAPSED RawJsonViewer over the payload, connected by a 1px neutral rail. Header carries the ConnectionBadge. D-09 rendering: a `flow_err` event renders a RED terminal frame in the body ("Flow failed — {error}." mono) with the partial timeline above it kept visible; when `conn==='errored'` (transport drop, no terminal) show the AMBER "Connection lost" header state + the muted reconnect copy + a Retry button that calls a passed `onRetry` prop (the parent wires this to the Plan-03 hook's `retry()`, which hydrates GET /runs/{id}/events + de-dups for the known runId — NOT a fresh /run/stream). Auto-scroll-pause (IC-3/D-03): while live and at-bottom, scroll to the newest frame (call scrollIntoView/scroll-to-bottom); on manual scroll-up set a `paused` flag and stop following + show the accent "Jump to latest" pill (bottom-center); clicking it scrolls to newest + resumes. No auto-scroll for instant-fill (a `mode==='replay'` prop disables following — Plan 05 uses it). Idle/empty states per the UI-SPEC copy ("Trigger a run to stream its event timeline."). Render ALL flowd strings (node names, outputs, error) as TEXT nodes — no dangerouslySetInnerHTML. Add TimelineView.test.tsx: feed a reducer model with the golden success + failure sequences; assert frame rows + per-node chips + connection badge states; assert flow_err shows the red in-body frame while conn errored shows the amber header + Retry (clicking Retry fires onRetry); assert auto-scroll intent fires when not paused and is skipped after a simulated manual scroll-up (scrollTo stubbed).</action>
  <verify>
    <automated>cd web && npx vitest run src/features/flow/components/TimelineView.test.tsx</automated>
  </verify>
  <done>Timeline renders frames with the correct per-frame/per-node/connection colors+icons+text; flow_err (red, in-body) and transport-drop (amber, header + Retry→onRetry) are visibly distinct per D-09; auto-scroll pauses on manual scroll-up and the Jump-to-latest pill resumes; all strings are text nodes.</done>
</task>

<task type="auto">
  <name>Task 2: RunTrigger (streamed→navigate to sub-route on X-Run-ID) + sync run + RunResultPanel, mounted in the flow detail</name>
  <files>web/src/features/flow/components/RunTrigger.tsx, web/src/features/flow/components/RunResultPanel.tsx, web/src/features/flow/hooks/useSyncRun.ts, web/src/features/flow/FlowDetailPage.tsx</files>
  <read_first>
    - web/src/features/memory/hooks/useMemoryMutations.ts (mutation + toast pattern)
    - 03-UI-SPEC.md IC-4 (one result surface for sync) + IC-7/S8 (run sub-route is the single live+replay render location) + S4 + Copywriting (Run / Run (sync) / Inputs / toasts)
    - 03-03-SUMMARY.md (useRunStream API: start(flowId,inputs,{onRunId}) / runId / conn)
    - 03-02-SUMMARY.md (the FlowDetailPage Definition-tab run-trigger slot + the TanStack Router navigate hook used Phase-2)
  </read_first>
  <behavior>
    - Inputs form: string→string key/value rows (flowd runRequest.Inputs map[string]string — values are strings only)
    - Primary "Run" (accent) → useRunStream.start(flowId, inputs, { onRunId }) → on X-Run-ID (onRunId) NAVIGATE to /flows/{flowId}/runs/{runId} where the live TimelineView renders (Plan 05). The Definition tab itself does NOT mount the live timeline.
    - Secondary "Run (sync)" (neutral) → useSyncRun → POST /run → {outputs}; the trigger disables + a loader spins on the RunResultPanel; outputs render into the RunResultPanel
    - A failed sync run renders its {error} in the RunResultPanel (red) + an error toast "Run failed — {status}: {error}."
    - Toasts: "Run started." (streamed) / "Run complete." (sync success)
  </behavior>
  <action>Build `useSyncRun.ts`: a mutation wrapping 03-01 `runSync` that toasts start/complete/failure (verbatim flowd message via parseFlowdError) and invalidates flowKeys.runs(flowId) so Slice C's history refreshes after a run. Build `RunResultPanel.tsx`: the sync outputs surface — renders `{outputs}` via RawJsonViewer on success and the `{error}` string (red) on failure; a loader while a sync run is in flight. (The streamed run's terminal flow_done outputs render in the sub-route's RunResultPanel in Plan 05 — this component is the shared surface used in both places.) Build `RunTrigger.tsx`: an Inputs key/value form (string→string) + the primary accent "Run" + the secondary neutral "Run (sync)". The primary "Run" calls `useRunStream.start(flowId, inputs, { onRunId: (runId) => navigate to /flows/{flowId}/runs/{runId} })` (use the Phase-2 TanStack Router navigate); "Run (sync)" calls useSyncRun. Mount RunTrigger + RunResultPanel (sync result) into the FlowDetailPage Definition tab (the slot Slice A left). Do NOT mount the live TimelineView/NodeStatusList in the Definition tab — those render at the run sub-route (Plan 05) after navigation. Render all strings as text nodes. Add the test by extending TimelineView.test.tsx or a focused RunTrigger assertion: the primary Run starts the stream and, on a supplied X-Run-ID, navigation to /flows/{id}/runs/{runId} fires (assert the navigate spy with the runId); sync run renders outputs in the RunResultPanel; sync failure renders the error + toast; the inputs form sends string→string. (Reducer/stream behavior is already covered in Plan 03.)</action>
  <verify>
    <automated>cd web && npx vitest run src/features/flow/components</automated>
  </verify>
  <done>Run (streamed) starts the stream and navigates to the run sub-route on X-Run-ID (D-08); Run (sync) renders {outputs} into the RunResultPanel; failure shows the error there + a verbatim toast; inputs are string→string; the live timeline is NOT mounted in the Definition tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console → BFF `/api/flow/.../run/stream` + `/run` | NO bearer / NO X-Console-* (BFF injects) |
| streamed event payloads (LLM/tool output) → DOM | Arbitrary upstream strings rendered into the live timeline |
| X-Run-ID open header → client navigation | A run id from the upstream response drives a route change to the sub-route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-V5 | Tampering / Elevation | TimelineView / NodeStatusList / RunResultPanel | mitigate | render ALL flowd event-payload strings (output/error/metadata/node names) as React TEXT nodes; raw-JSON viewer shows escaped text; NEVER dangerouslySetInnerHTML — heightened here because payloads stream from arbitrary node outputs (RESEARCH V5). Asserted by TimelineView.test rendering a payload with markup chars and checking it is escaped. |
| T-03-08 | Spoofing / confused-deputy | run/stream + run calls | mitigate | RunTrigger/useSyncRun send NO Authorization / X-Console-* (the 03-01 client/stream already omit them); no auth header added at the call site |
| T-03-06 | DoS / resource | streamed run lifecycle | mitigate | useRunStream (Plan 03) aborts on terminal + unmount; navigating to the sub-route mounts the hook there and the Definition tab unmount aborts any stray stream (flowd r.Context() cancels) |
| T-03-09 | Repudiation / operator-confusion | D-09 signal distinction | mitigate | red flow_err in-body vs amber "Connection lost" in-header are different colors in different places (IC-6); the transport-drop Retry recovers via /events hydrate (a known-runId re-tail), never a new run — asserted distinct in TimelineView.test so a failed flow is never read as a dropped pipe nor silently duplicated |
| T-03-12 | Tampering | X-Run-ID → navigate | mitigate | the run id only builds the local /flows/{id}/runs/{runId} route param; not interpolated into markup or a fetch auth header |
| T-03-SC | Tampering | npm installs | mitigate | no installs (RESEARCH); reuses existing primitives + shadcn blocks |
</threat_model>

<verification>
- `cd web && npx vitest run src/features/flow` — Slice B tests green (reducer/hook from Plan 03 + view/trigger here).
- `cd web && npx vitest run` — full suite green.
- MANUAL GATE (closes Phase-1 BFF-03 Part 2 — 03-VALIDATION "Manual-Only Verifications"): with the compose stack up (flowd `New(cfg)` + BFF + fronting nginx), trigger a streamed run through the deployed stack and confirm frames render INCREMENTALLY (not batched) at the run sub-route and idle-survival across a slow node (raised proxy_read_timeout). Document the required LB idle-timeout for Phase 6.
</verification>

<success_criteria>
- TimelineView/NodeStatusList/ConnectionBadge/RunResultPanel built; streamed run renders incrementally (at the sub-route, Plan 05) with per-node status, connection badge, and auto-scroll-pause + jump-to-latest.
- The streamed Run trigger navigates to /flows/{id}/runs/{runId} on X-Run-ID; the live timeline is NOT in the Definition tab (D-08).
- Sync run outputs render into the RunResultPanel; failure shows the error there + a toast.
- flow_err (red, in-body) and transport drop (amber, header + Retry→/events-hydrate) are visibly distinct (D-09 / IC-6).
- All upstream strings rendered as text nodes; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-flow-console/03-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
