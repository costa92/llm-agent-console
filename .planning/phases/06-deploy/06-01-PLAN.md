---
phase: 06-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/nginx.conf
  - deploy/docker-compose.yml
  - internal/router/router.go
  - internal/router/router_test.go
  - .gitignore
autonomous: true
requirements: [SC-1, SC-2]

must_haves:
  truths:
    - "nginx regex matches /api/flow/runs/{id}/replay — replay SSE goes through the unbuffered SSE block, not the 60s-timeout REST block (GAP-1 fix)"
    - "Both bff and nginx compose services carry restart: unless-stopped (GAP-2 fix, SC-1)"
    - "BFF bff service has a dedicated healthcheck that probes :8090/healthz directly (GAP-5 optional but planned for correctness)"
    - "A synthetic GET /api/replay/test endpoint exists in the BFF, mirroring /api/stream/test, so sse-proof.sh PART 3 can prove the replay regex without a live flowd (SC-2 prereq)"
    - "config/config.prod.yaml added to .gitignore so a future bind-mounted prod config is never accidentally committed"
  artifacts:
    - path: "deploy/nginx.conf"
      provides: "SSE location regex broadened to (stream|replay)"
      contains: "stream|replay"
    - path: "deploy/docker-compose.yml"
      provides: "restart: unless-stopped on bff and nginx; bff healthcheck"
      contains: "restart: unless-stopped"
    - path: "internal/router/router.go"
      provides: "GET /api/replay/test synthetic endpoint registered"
      contains: "/api/replay/test"
    - path: ".gitignore"
      provides: "config/config.prod.yaml excluded"
      contains: "config.prod.yaml"
  key_links:
    - from: "deploy/nginx.conf"
      to: "bff:8090"
      via: "location ~* ^/api/.*(stream|replay) { proxy_buffering off }"
      pattern: "stream|replay"
    - from: "internal/router/router.go"
      to: "syntheticReplaySSEHandler"
      via: "mux.HandleFunc(\"GET /api/replay/test\", syntheticReplaySSEHandler)"
      pattern: "/api/replay/test"
---

<objective>
Slice 1 — Foundation fixes. Close the two blocking gaps (GAP-1 nginx regex + GAP-2 restart policy)
and create the synthetic BFF replay endpoint needed by the SC-2 verification in Plan 06-02.

Purpose: Without GAP-1 fixed, replay SSE streams hit the 60s-timeout REST block and silently
buffer/time out — SC-2 cannot pass. Without GAP-2, the long-lived service requirement (SC-1)
is not met. The synthetic /api/replay/test endpoint lets sse-proof.sh PART 3 (written in 06-02)
prove the (stream|replay) regex fires without requiring a live flowd instance.

Output: corrected deploy/nginx.conf, promoted deploy/docker-compose.yml with restart policies,
syntheticReplaySSEHandler in router.go, .gitignore hardened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-deploy/06-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md

<read_first>
@deploy/nginx.conf
@deploy/docker-compose.yml
@internal/router/router.go
@.gitignore
</read_first>

<interfaces>
<!-- Key contracts the executor uses. Extracted from codebase. -->

From internal/router/router.go — existing synthetic SSE handler to mirror for replay:
  const syntheticTicks = 30
  func syntheticSSEHandler(w http.ResponseWriter, r *http.Request)
    // Sets: Content-Type: text/event-stream, Cache-Control: no-cache no-transform,
    //       X-Accel-Buffering: no, Connection: keep-alive
    // Emits: event: tick / data: {...} every 1s for syntheticTicks ticks, then event: done
    // Handles: r.Context().Done() for client disconnect
  // Registered as: mux.HandleFunc("GET /api/stream/test", syntheticSSEHandler)

From deploy/nginx.conf — the broken regex to fix (line 32):
  location ~* ^/api/.*stream {   // BEFORE: misses /api/flow/runs/{id}/replay
  location ~* ^/api/.*(stream|replay) {  // AFTER: covers all 4 SSE paths

From deploy/docker-compose.yml — services that need restart policy:
  services:
    bff:   // add: restart: unless-stopped + healthcheck
    nginx: // add: restart: unless-stopped

From 06-RESEARCH.md GAP-5 — recommended bff healthcheck:
  healthcheck:
    test: ["CMD", "wget", "-qO-", "http://localhost:8090/healthz"]
    interval: 5s
    retries: 6
  // alpine:3.20 ships wget not curl — use wget for consistency
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nginx SSE regex + promote compose to long-lived service (GAP-1, GAP-2)</name>
  <files>deploy/nginx.conf, deploy/docker-compose.yml, .gitignore</files>
  <action>
    In deploy/nginx.conf, change line 32 from:
      location ~* ^/api/.*stream {
    to:
      location ~* ^/api/.*(stream|replay) {
    Update the comment above it (line 31) to accurately describe all four matched paths:
    /api/stream/test, /api/flow/*/run/stream, /api/flow/runs/*/replay, /api/chat/stream.
    Do NOT alter any other directive in the SSE block or anywhere else in the file
    (proxy_buffering off / gzip off / proxy_read_timeout 3600s / proxy_http_version 1.1 /
    Connection '' / proxy_pass_header X-Accel-Buffering are all correct per 06-RESEARCH).

    In deploy/docker-compose.yml, add restart: unless-stopped to both the bff and nginx
    service definitions (per GAP-2). Also add a dedicated healthcheck to the bff service
    (per GAP-5 — optional in research, included here for correctness):
      healthcheck:
        test: ["CMD", "wget", "-qO-", "http://localhost:8090/healthz"]
        interval: 5s
        retries: 6
    Keep all other keys (build context, hostname, expose, ports, volumes, nginx healthcheck)
    exactly as they are — surgical edits only.

    In .gitignore, add config/config.prod.yaml on a new line (security: ensure a future
    bind-mounted prod config with real tokens is never accidentally committed). The existing
    gitignore already has config/config.local.yaml; add config.prod.yaml alongside it.
  </action>
  <verify>
    <automated>grep -c '(stream|replay)' deploy/nginx.conf && grep -c 'restart: unless-stopped' deploy/docker-compose.yml && grep -c 'config.prod.yaml' .gitignore</automated>
  </verify>
  <done>
    deploy/nginx.conf contains the (stream|replay) regex. deploy/docker-compose.yml has
    restart: unless-stopped on both services and a bff healthcheck. .gitignore contains
    config/config.prod.yaml. No other lines changed in nginx.conf.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add synthetic GET /api/replay/test endpoint to BFF (SC-2 prereq)</name>
  <files>internal/router/router.go, internal/router/router_test.go</files>
  <behavior>
    - GET /api/replay/test returns Content-Type: text/event-stream and X-Accel-Buffering: no
    - The handler emits at least 3 sequential "event: tick" frames (same format as /api/stream/test)
    - The handler emits a final "event: done" frame and closes normally
    - Client disconnect via context cancellation stops the handler (no goroutine leak)
    - The route is registered alongside the existing /api/stream/test route
  </behavior>
  <action>
    In internal/router/router.go, add a syntheticReplaySSEHandler function that is a structural
    copy of syntheticSSEHandler — identical SSE transport headers (Content-Type: text/event-stream,
    Cache-Control: no-cache no-transform, X-Accel-Buffering: no, Connection: keep-alive),
    identical tick emission loop, identical r.Context().Done() disconnect handling. The only
    difference from syntheticSSEHandler is the function name; the behavior is identical so
    sse-proof.sh PART 3 can use the same assertion logic as PART 1/2. Register it in New():
      mux.HandleFunc("GET /api/replay/test", syntheticReplaySSEHandler)
    Place it next to the existing /api/stream/test line. Do NOT modify syntheticSSEHandler,
    syntheticTicks, or any other existing function.

    In internal/router/router_test.go (create if not present, append if present), add
    TestSyntheticReplaySSEHandler: use httptest.NewRecorder + httptest.NewRequest("GET",
    "/api/replay/test", nil). Because NewRecorder is not a streaming Flusher, verify the
    handler returns 200 and sets the correct Content-Type and X-Accel-Buffering headers.
    For the tick-emission behavior, use a context.WithCancel — cancel after receiving the
    first tick frame to stop the handler — assert the response body contains "event: tick".
    This is sufficient for a unit test; the full through-nginx streaming proof is in 06-02
    sse-proof.sh PART 3.
  </action>
  <verify>
    <automated>GOWORK=off go test ./internal/router/ -run TestSyntheticReplay -count=1 -v</automated>
  </verify>
  <done>
    GOWORK=off go build ./... compiles cleanly. TestSyntheticReplaySSEHandler passes.
    GET /api/replay/test is registered in New(). The handler emits SSE headers and tick frames.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → nginx:80 | All traffic enters through the fronting nginx; nginx routes /api/* to BFF |
| nginx → bff:8090 | Internal compose network; BFF trusts nginx as its only caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | config/config.prod.yaml | mitigate | Add to .gitignore so prod secrets (flowd_token, operator_token) are never committed; operator creates this file on the host only. |
| T-06-02 | Tampering | deploy/nginx.conf regex change | accept | The (stream|replay) regex only broadens which paths get proxy_buffering off — it does not open new proxy targets. bff:8090 is the sole upstream; no SSRF surface added. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — no slopcheck gate needed. |
</threat_model>

<verification>
- `grep -c '(stream|replay)' deploy/nginx.conf` returns 1 (the updated SSE location line).
- `grep -c 'restart: unless-stopped' deploy/docker-compose.yml` returns 2 (both services).
- `grep -c 'config.prod.yaml' .gitignore` returns at least 1.
- `GOWORK=off go build ./...` exits 0.
- `GOWORK=off go test ./internal/router/ -run TestSyntheticReplay -count=1` passes.
- `GOWORK=off go test ./... -count=1` passes (no regressions).
</verification>

<success_criteria>
- GAP-1 closed: nginx SSE regex is ^/api/.*(stream|replay) — replay path no longer falls through to 60s-timeout REST block.
- GAP-2 closed: both compose services have restart: unless-stopped (SC-1 long-lived service requirement met at the config level).
- GAP-5 addressed: bff service has a dedicated healthcheck (defense-in-depth; nginx healthcheck already covers the full path).
- Synthetic /api/replay/test endpoint exists and is tested — prerequisite for sse-proof.sh PART 3 in 06-02.
- .gitignore hardened: config/config.prod.yaml will not be committed.
</success_criteria>

<output>
Create `.planning/phases/06-deploy/06-01-SUMMARY.md` when done.
</output>
