---
phase: 02-memory-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/features/memory/api/schemas.ts
  - web/src/features/memory/api/client.ts
  - web/src/features/memory/api/queries.ts
  - web/src/features/memory/api/schemas.test.ts
  - web/src/features/memory/api/client.test.ts
  - web/src/test/mocks/memory-gateway.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-03
  - MEM-04

must_haves:
  truths:
    - "schemas.ts exports zod schemas recallHitSchema, recallResponseSchema, memoryItemSchema, writeRecordSchema, patchFieldsSchema, gatewayErrorSchema, and a memoryConflictDetailsSchema parsing {memory_id, expected_version, current_version}"
    - "writeRecordSchema requires kind ∈ {working, episodic, semantic} and content min length 1; rejects empty content and unknown kind (D-07)"
    - "patchFieldsSchema marks content/category/tags/importance all optional but requires at least one key present; rejects an empty object (D-07)"
    - "client.ts base path constant equals '/api/memory' (Phase-1 BFF prefix, confirmed from executed Phase-1 router StripPrefix '/api/memory'; A2 resolved)"
    - "client.ts recall() POSTs /api/memory/recall/unified with body {scope:{}, query, top_k}; never sends sort/offset/page/cursor (recall has no such params — DisallowUnknownFields 400)"
    - "client.ts sends body scope as {} on every call and never sends client-trusted X-Tenant-Id/X-User-Id headers (D-12 / V4 scope confinement); identity rides on X-Console-* injected by Phase-1 makeApiFetcher"
    - "client.ts sets Content-Type: application/json on every mutating call including DELETE (which carries a body)"
    - "client.ts patch(), pin(), unpin(), disable(), enable(), del() all send expected_version in the body; write() sends idempotency_key (fresh UUID) + record, no expected_version"
    - "client.ts parseGatewayError() reads the {error:{code,message,request_id,retryable,details}} envelope and a 409 with code memory_conflict is distinguishable by callers (carries details.current_version)"
    - "queries.ts exports a memoryKeys factory: recall(params)=>['recall',params], item(id)=>['memory-item',id]"
    - "queries.ts exports useRecallQuery(params) and useItemQuery(id) using the makeApiFetcher from Phase-1 OperatorContext so X-Console-* headers are injected"
    - "test mock memory-gateway.ts returns the documented contract shapes including a non-empty hits case, an empty-hits ({hits:[]}) case, and a 409 memory_conflict envelope"
  artifacts:
    - path: "web/src/features/memory/api/schemas.ts"
      provides: "zod schemas for recall response, item record, write/patch payloads, gateway error envelope + 409 conflict details"
      exports: ["recallHitSchema", "recallResponseSchema", "memoryItemSchema", "writeRecordSchema", "patchFieldsSchema", "gatewayErrorSchema"]
      min_lines: 40
    - path: "web/src/features/memory/api/client.ts"
      provides: "typed /api/memory/* fetchers (recall, getItem, write, patch, pin, unpin, disable, enable, del) with scope:{} + expected_version threading + parseGatewayError"
      exports: ["recall", "getItem", "write", "patch", "pin", "unpin", "disable", "enable", "del", "parseGatewayError"]
    - path: "web/src/features/memory/api/queries.ts"
      provides: "memoryKeys query-key factory + useRecallQuery + useItemQuery bound to Phase-1 makeApiFetcher"
      exports: ["memoryKeys", "useRecallQuery", "useItemQuery"]
    - path: "web/src/test/mocks/memory-gateway.ts"
      provides: "fetch mock handlers returning contract shapes incl. empty-hits + 409 conflict"
  key_links:
    - from: "web/src/features/memory/api/client.ts recall"
      to: "/api/memory/recall/unified"
      via: "apiFetch POST with {scope:{}, query, top_k}"
      pattern: "recall/unified"
    - from: "web/src/features/memory/api/client.ts mutating calls"
      to: "expected_version in request body"
      via: "every patch/pin/unpin/disable/enable/del sends expected_version"
      pattern: "expected_version"
    - from: "web/src/features/memory/api/queries.ts useRecallQuery"
      to: "Phase-1 makeApiFetcher (X-Console-* injection)"
      via: "useOperatorContext + makeApiFetcher"
      pattern: "makeApiFetcher"
---

<objective>
Wave 0 foundation for the Memory Console: the typed `/api/memory/*` client, the zod schemas derived from the verified gateway wire contract, the TanStack Query key factory + recall/item query hooks, and the test mock harness (fixtures + handlers including the empty-hits and 409-conflict cases). Every later slice (A recall→render, B drawer, C lifecycle) builds on these artifacts.

Purpose: Lock the gateway contract into typed, tested code once so the slices wire UI to a known surface, not to ad-hoc fetches. The `expected_version` OCC threading, the `scope:{}` body convention, the `/api/memory` base path (Phase-1 BFF prefix), and the lean-vs-full mutation response shapes are all encoded here as the single source of truth.

Output: `web/src/features/memory/api/{schemas,client,queries}.ts` + their unit tests + `web/src/test/mocks/memory-gateway.ts`. No UI in this plan — pure data layer + test infra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-console/02-RESEARCH.md
@.planning/phases/02-memory-console/02-VALIDATION.md
</context>

<interfaces>
<!-- Phase-1 substrate this plan consumes. Confirmed from executed Phase-1 plans 01-03 (BFF prefix) and 01-04 (api fetcher). -->

BFF route prefix (Phase-1 plan 01-03, router StripPrefix "/api/memory" → gateway "/memory/*"):
  Console base path = "/api/memory". A2 / Open-Q1 RESOLVED — do NOT re-investigate; reuse "/api/memory".
  Map: /api/memory/recall/unified, /api/memory/write, /api/memory/items/{id} (GET/PATCH/DELETE),
       /api/memory/items/{id}/{pin|unpin|disable|enable} (POST).

From web/src/lib/api.ts (Phase-1 plan 01-04):
  type OperatorContext = { tenantId: string; userId: string; projectId: string; sessionId: string; setContext: (p: Partial<...>) => void }
  function makeApiFetcher(ctx: OperatorContext): (path: string, init?: RequestInit) => Promise<Response>
  // makeApiFetcher injects X-Console-Tenant/User/Project/Session headers when set. NEVER inject X-Tenant-Id/X-User-Id from the client.

From web/src/app/OperatorContextProvider.tsx (Phase-1 plan 01-04):
  function useOperatorContext(): OperatorContext

Gateway golden wire fixtures (ready-made test data; copy shapes verbatim):
  ../llm-agent-memory-gateway/internal/httpapi/testdata/wire/{recall_unified_response,get_memory_item_response,pin_memory_response,disable_memory_response,patch_memory_response,write_memory_response,delete_memory_response,error_response}.json
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zod schemas + test mock harness from the verified gateway contract</name>
  <files>web/src/features/memory/api/schemas.ts, web/src/features/memory/api/schemas.test.ts, web/src/test/mocks/memory-gateway.ts</files>

  <read_first>
    - `.planning/phases/02-memory-console/02-RESEARCH.md` "THE GATEWAY CONTRACT" (recall request/response schema, GetMemoryItemResponse, WriteRecordPayload, PatchMemoryFields, mutation response table, error envelope + code→status map, the `expected_version` section)
    - `../llm-agent-memory-gateway/internal/httpapi/testdata/wire/recall_unified_response.json` + `get_memory_item_response.json` + `error_response.json` (golden shapes — the authoritative field set; the `error_response.json` carries the 409 `details:{memory_id,expected_version,current_version}`)
    - `.planning/phases/02-memory-console/02-VALIDATION.md` Wave 0 Requirements (fixtures, mock handlers incl. 409 + empty-hits)
  </read_first>

  <behavior>
    - Test: writeRecordSchema.parse rejects `{kind:"semantic", content:""}` (content min length 1) and rejects `{kind:"bogus", content:"x"}` (kind not in enum); accepts `{kind:"episodic", content:"hi"}`
    - Test: patchFieldsSchema rejects `{}` (no keys) with a clear error; accepts `{content:"x"}`; accepts `{tags:["a"]}`; treats kind/source/pinned/disabled as NOT patchable (parse strips or rejects them — choose reject so the editor surfaces the mistake)
    - Test: recallResponseSchema.parse accepts the golden recall_unified_response.json shape (hits[] with memory_id, kind, score, version, content, pinned, disabled, metadata) and accepts `{hits:[]}` (empty)
    - Test: memoryItemSchema.parse accepts the golden get_memory_item_response.json shape (memory_id, kind, version, content, pinned, disabled; importance optional; tags optional)
    - Test: gatewayErrorSchema.parse accepts the golden error_response.json shape and a 409 envelope where error.code === "memory_conflict" exposes details.current_version
  </behavior>

  <action>
    Create `web/src/features/memory/api/schemas.ts` exporting zod schemas that mirror the verified gateway contract (RESEARCH "THE GATEWAY CONTRACT").

    Define `kindEnum` = zod enum of "working" | "episodic" | "semantic". Define `recallHitSchema` with fields: memory_id (string), kind (kindEnum), score (number), version (number), content (string), tags (array of string, optional), source (string, optional), category (string, optional), pinned (boolean), disabled (boolean), metadata (object with matched_by string optional + token_cost_estimate number optional, passthrough-tolerant). Define `recallResponseSchema` = object with hits (array of recallHitSchema) + trace (object, optional, loose). Define `memoryItemSchema` (the GET item) with memory_id, kind, version, content, tags (optional), source (optional), category (optional), importance (number, optional), pinned, disabled.

    Define `writeRecordSchema` (the operator-edited `record` object only — NOT the envelope): kind (kindEnum, required), content (string min(1), required, message "content: required."), source (string optional), category (string optional), tags (array of string optional), importance (number optional), pinned (boolean optional). Define `patchFieldsSchema`: content (string optional), category (string optional), tags (array of string optional), importance (number optional) — then refine to require at least one key present (message "patch: provide at least one of content, category, tags, importance."), and reject unknown keys (strict) so an operator pasting kind/source/pinned gets a clear error that those are not patchable.

    Define `gatewayErrorSchema` = object `{ error: { code: string, message: string, request_id: string, retryable: boolean, details: record(unknown) } }` (details loose). Define `memoryConflictDetailsSchema` = object `{ memory_id: string, expected_version: number, current_version: number }` to narrow `error.details` when code === "memory_conflict".

    Create `web/src/test/mocks/memory-gateway.ts` exporting fetch-mock handler helpers and the canonical fixture objects (recallNonEmpty, recallEmpty = {hits:[]}, itemFixture, pinResponse {memory_id,version,pinned}, disableResponse {memory_id,version,disabled}, patchResponse {memory_id,version}, writeResponse {memory:{memory_id,version,status}}, deleteResponse {memory_id,deleted,version}, conflict409 envelope). Source each fixture's shape from the golden wire JSON. Export a `installMemoryFetchMock(routes)` helper that stubs global fetch (via `vi.fn`) and matches on method+path, returning the configured Response (status + JSON body), so component/hook tests can drive recall/empty/409 paths deterministically.

    Create `web/src/features/memory/api/schemas.test.ts` covering all five behavior cases above with `zod`'s safeParse/parse.
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run src/features/memory/api/schemas.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/memory/api/schemas.test.ts` exits 0; all five behavior cases green
    - `grep -c 'working' web/src/features/memory/api/schemas.ts` returns >= 1 (kind enum) and the enum includes episodic + semantic
    - `grep -c 'min(1)\|min(1,' web/src/features/memory/api/schemas.ts` returns >= 1 (content non-empty rule)
    - `grep -c 'memory_conflict\|current_version' web/src/features/memory/api/schemas.ts` returns >= 1 (409 details schema present)
    - `grep -v '^//' web/src/test/mocks/memory-gateway.ts | grep -c 'hits: \[\]\|hits:\[\]' ` returns >= 1 (empty-hits fixture present)
    - `grep -c '409\|memory_conflict' web/src/test/mocks/memory-gateway.ts` returns >= 1 (409 conflict fixture present)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>zod schemas mirror the verified gateway contract (recall, item, write, patch, error, 409 details); write/patch validation rules match D-07; the mock harness exposes non-empty, empty-hits, and 409 fixtures from the golden wire shapes. Reusable as test fixtures by all later slices.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: typed /api/memory client + query-key factory + recall/item hooks</name>
  <files>web/src/features/memory/api/client.ts, web/src/features/memory/api/queries.ts, web/src/features/memory/api/client.test.ts</files>

  <read_first>
    - `.planning/phases/02-memory-console/02-RESEARCH.md` "Code Examples" (recall/pin/delete fetchers — scope:{}, expected_version, Content-Type on DELETE), "THE GATEWAY CONTRACT" (routes, auth scope rule MergeAuthoritativeScope → send scope:{}, error envelope), "expected_version" section (every mutation carries it; write uses idempotency_key instead), the D-03 plan directive (no sort/offset; only query/top_k re-query), Common Pitfalls 2/4/6
    - `.planning/phases/01-foundation/01-04-PLAN.md` <interfaces> + Task 1 action (makeApiFetcher signature; X-Console-* injection; api.ts is the only header-injection point)
    - `.planning/phases/01-foundation/01-03-PLAN.md` frontmatter + Task 2 action (router StripPrefix "/api/memory" — confirms the console base path is "/api/memory")
    - `.planning/phases/02-memory-console/02-RESEARCH.md` "Pattern: query-key factory keyed on URL params" + "Pattern: reflect-from-response vs refetch"
  </read_first>

  <behavior>
    - Test: recall(apiFetch, {query:"x", top_k:8}) calls apiFetch with path "/api/memory/recall/unified", method POST, a JSON body containing {"scope":{}, "query":"x", "top_k":8}, and Content-Type application/json; body contains NO sort/offset/page/cursor keys
    - Test: recall throws a parsed gateway error (from parseGatewayError) when the mock returns a non-2xx envelope
    - Test: getItem(apiFetch, "mem_1") calls GET "/api/memory/items/mem_1" and returns a memoryItemSchema-valid object
    - Test: pin(apiFetch, "mem_1", 7) POSTs "/api/memory/items/mem_1/pin" with body {"scope":{}, "expected_version":7} and returns {memory_id,version,pinned}
    - Test: patch(apiFetch, "mem_1", {content:"y"}, 7) PATCHes "/api/memory/items/mem_1" with body containing expected_version:7, a patch object {content:"y"}, scope:{}, and a non-empty idempotency_key (fresh UUID)
    - Test: del(apiFetch, "mem_1", 7) sends method DELETE to "/api/memory/items/mem_1" WITH Content-Type application/json and a body {"scope":{}, "expected_version":7}
    - Test: parseGatewayError(response) for a 409 with code memory_conflict returns an object exposing code==="memory_conflict" and details.current_version
    - Test: memoryKeys.recall({query:"x",top_k:8}) === ['recall',{query:"x",top_k:8}] and memoryKeys.item("mem_1") === ['memory-item','mem_1']
  </behavior>

  <action>
    Create `web/src/features/memory/api/client.ts`. Export `const MEMORY_BASE = '/api/memory'`. Each fetcher takes the Phase-1 `apiFetch` (the function returned by `makeApiFetcher`) as its first arg so X-Console-* injection is inherited (do NOT construct headers for identity here).

    Implement: `recall(apiFetch, params)` → POST `${MEMORY_BASE}/recall/unified` with body `{ scope: {}, query: params.query, top_k: params.top_k ?? 8, consistency_level: params.consistency_level }`; set Content-Type application/json; on `!res.ok` `throw await parseGatewayError(res)`; else `recallResponseSchema.parse(await res.json())`. Send ONLY the documented request fields — never sort/offset/page/cursor (gateway DisallowUnknownFields → 400). `getItem(apiFetch, id)` → GET `${MEMORY_BASE}/items/${id}`; parse with memoryItemSchema. `write(apiFetch, record)` → POST `${MEMORY_BASE}/write` with body `{ idempotency_key: crypto.randomUUID(), scope: {}, record }`; Content-Type application/json. `patch(apiFetch, id, patch, expected_version)` → PATCH `${MEMORY_BASE}/items/${id}` with body `{ idempotency_key: crypto.randomUUID(), scope: {}, expected_version, patch }`. `pin/unpin/disable/enable(apiFetch, id, expected_version)` → POST `${MEMORY_BASE}/items/${id}/{action}` with body `{ scope: {}, expected_version }`. `del(apiFetch, id, expected_version)` → DELETE `${MEMORY_BASE}/items/${id}` with body `{ scope: {}, expected_version }` AND Content-Type application/json (this DELETE carries a body — gateway 400s without the header). Every mutating fetcher sets Content-Type application/json and throws parseGatewayError on `!res.ok`.

    Implement `parseGatewayError(res)`: read the JSON, parse with gatewayErrorSchema (tolerant fallback if the body is not the envelope — return a synthetic `{error:{code:'transport_error',message:res.statusText,...}}`), attach `httpStatus = res.status`. Expose enough that a caller can branch on `err.error.code === 'memory_conflict'` and read `err.error.details.current_version` (narrow via memoryConflictDetailsSchema). Return (do not throw) a normalized error object so call sites `throw` it.

    Create `web/src/features/memory/api/queries.ts`. Export `memoryKeys = { recall: (params) => ['recall', params] as const, item: (id) => ['memory-item', id] as const }`. Export `useRecallQuery(params)` and `useItemQuery(id)` hooks: read `useOperatorContext()`, build `apiFetch = makeApiFetcher(ctx)`, and use TanStack Query `useQuery` with `queryKey: memoryKeys.recall(params)` / `memoryKeys.item(id)` and `queryFn` calling `recall(apiFetch, params)` / `getItem(apiFetch, id)`. `useRecallQuery` must be `enabled` only when params.query is non-empty (no doomed empty-query POST); `useItemQuery` enabled only when id is set. The recall query re-runs only when query/top_k/consistency_level change (they are in the key) — sort/page/state-filter are client-side and MUST NOT be in the key (D-03/D-13).

    Create `web/src/features/memory/api/client.test.ts` covering all behavior cases using the `installMemoryFetchMock` harness from Task 1. Pass a fake `apiFetch` that records path/method/body/headers so the tests can assert the request shape.
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run src/features/memory/api/client.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/memory/api/client.test.ts` exits 0; all behavior cases green
    - `grep -c "'/api/memory'" web/src/features/memory/api/client.ts` returns >= 1 (base path is the Phase-1 prefix)
    - `grep -c 'scope: {}\|scope:{}' web/src/features/memory/api/client.ts` returns >= 1 (empty body scope; identity never client-trusted)
    - `grep -cE 'sort|offset|cursor|"page"' web/src/features/memory/api/client.ts` returns 0 (recall has no such params)
    - `grep -c 'expected_version' web/src/features/memory/api/client.ts` returns >= 5 (patch/pin/unpin/disable/enable/del all thread it)
    - `grep -c 'application/json' web/src/features/memory/api/client.ts` returns >= 1 and a DELETE path sets it (body-bearing DELETE)
    - `grep -c 'X-Tenant-Id\|X-User-Id' web/src/features/memory/api/client.ts` returns 0 (no client-trusted identity headers)
    - `grep -c 'makeApiFetcher' web/src/features/memory/api/queries.ts` returns >= 1 (hooks inherit X-Console-* injection)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Typed client with scope:{} + expected_version threading + body-bearing DELETE + parseGatewayError that surfaces 409 memory_conflict; query-key factory + recall/item hooks bound to Phase-1 makeApiFetcher; no client-trusted identity headers, no recall sort/offset params. All unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA client → BFF /api/memory/* | Console sends body scope:{} + X-Console-* (via Phase-1 makeApiFetcher); BFF re-materializes authoritative X-Tenant-Id/X-User-Id |
| Gateway JSON response → zod parse | Untrusted upstream shape narrowed by zod before it reaches UI state |
| parseGatewayError → caller | Error envelope (incl. request_id, details) surfaced to operator; internal tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing (confused deputy) | client.ts identity headers | mitigate [BLOCKING] | client.ts sends body scope:{} and NEVER sets X-Tenant-Id/X-User-Id; identity rides only on X-Console-* injected by Phase-1 makeApiFetcher (BFF authoritative). Acceptance grep asserts zero X-Tenant-Id/X-User-Id in client.ts |
| T-02-02 | Tampering | recall request with unknown params | mitigate | client.ts sends only documented recall fields; sort/offset/cursor are absent (gateway DisallowUnknownFields would 400). Acceptance grep asserts none present |
| T-02-03 | Input validation (V5) | upstream JSON parsed into UI state | mitigate | every response narrowed by zod (recallResponseSchema/memoryItemSchema); malformed upstream shapes fail fast instead of corrupting state |
| T-02-04 | Information disclosure | parseGatewayError surfaces request_id/details | accept | Internal operator tool; operator needs the real upstream error + request_id for support; not logged to third parties |
| T-02-SC | Tampering | npm/package installs | accept | No new packages — all deps installed + audited in Phase-1 (RESEARCH Package Legitimacy Audit: not applicable, zero installs this phase) |
</threat_model>

<verification>
## Wave 1 Phase Checks

1. `cd web && npx vitest run src/features/memory/api --run` — all schema + client tests PASS
2. `cd web && npx tsc --noEmit` — exits 0
3. `grep -c "'/api/memory'" web/src/features/memory/api/client.ts` — >= 1 (Phase-1 prefix reused; A2 resolved)
4. `grep -c 'X-Tenant-Id\|X-User-Id' web/src/features/memory/api/client.ts` — 0 (V4 scope confinement)
5. `grep -cE 'sort|offset|cursor' web/src/features/memory/api/client.ts` — 0 (recall has no sort/offset)
6. `grep -c 'expected_version' web/src/features/memory/api/client.ts` — >= 5 (OCC threading)
</verification>

<success_criteria>
- zod schemas mirror the verified gateway contract; write/patch rules match D-07
- Client uses /api/memory base path, scope:{} bodies, expected_version on every mutation, idempotency_key on write/patch, Content-Type on body-bearing DELETE
- parseGatewayError surfaces 409 memory_conflict as a distinguishable, recoverable error with details.current_version
- Query hooks inherit X-Console-* injection from Phase-1 makeApiFetcher; no client-trusted identity headers
- Mock harness exposes non-empty, empty-hits, and 409 fixtures; reusable by all later slices
- All Wave-1 tests + tsc green
</success_criteria>

<output>
Create `.planning/phases/02-memory-console/02-01-SUMMARY.md` when done
</output>
