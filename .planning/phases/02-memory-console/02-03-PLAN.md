---
phase: 02-memory-console
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - web/src/features/memory/components/ItemDrawer.tsx
  - web/src/features/memory/MemoryPage.tsx
  - web/src/features/memory/components/ItemDrawer.test.tsx
autonomous: true
requirements:
  - MEM-02

must_haves:
  truths:
    - "When ?item={id} is set in the URL, an item detail drawer (shadcn sheet) slides over the results list (list stays visible/dimmed behind) — D-04/D-05/IC-2"
    - "The drawer fetches GET /api/memory/items/{id} via useItemQuery and renders the item's fields (memory_id, kind, version, content, tags, source, category, importance, pinned, disabled) — MEM-02"
    - "The drawer renders memory_id via Phase-1 CopyableId and the full item via the Phase-1 RawJsonViewer (raw JSON shown as escaped text) — MEM-02 / SHELL-07"
    - "Reload with ?item={id} reopens the drawer to the same item; browser-back closes it (URL-synced — D-05/IC-2)"
    - "Drawer five-state: loading (item GET in flight) → ready (rendered fields + raw JSON); a 404 not_found renders the ERROR state inside the drawer, not an empty state (IC-2)"
    - "Closing the drawer clears ?item from the URL without re-running recall (D-09 — search is operator-initiated)"
    - "The drawer renders a lifecycle action region + a 'Patch' button as placeholders wired by plans 04/05 (this plan ships the host + detail; actions are stubs/TODOs here)"
    - "Honors decisions D-04 (side-drawer detail, not a full route) and D-05 (open item synced to ?item URL search param)"
  artifacts:
    - path: "web/src/features/memory/components/ItemDrawer.tsx"
      provides: "sheet drawer opened by ?item; GET item via useItemQuery; rendered fields + CopyableId + RawJsonViewer + action-region host; five-state (loading/ready/404-error)"
      min_lines: 40
  key_links:
    - from: "web/src/features/memory/components/ItemDrawer.tsx open state"
      to: "?item={id} URL search param"
      via: "useMemorySearchParams item getter; close → clear item"
      pattern: "item"
    - from: "web/src/features/memory/components/ItemDrawer.tsx"
      to: "Phase-1 useItemQuery (02-01) GET /api/memory/items/{id}"
      via: "useItemQuery(id) enabled when item set"
      pattern: "useItemQuery"
    - from: "web/src/features/memory/components/ItemDrawer.tsx"
      to: "Phase-1 RawJsonViewer + CopyableId"
      via: "import from components/primitives"
      pattern: "RawJsonViewer"
---

<objective>
Slice B — the URL-synced item detail drawer. Opening a result row (or loading a URL with `?item={id}`) slides a shadcn `sheet` over the results list and fetches `GET /api/memory/items/{id}`, rendering the item's fields plus the Phase-1 raw-JSON viewer and copyable id. Browser-back closes it; reload reopens it; the link is shareable — recovering the deep-link value given up by choosing a drawer over a full route.

Purpose: After this slice an operator can inspect any hit's full record without leaving the results list, and deep-link straight to an item. The drawer is also the host surface for the lifecycle actions and the patch editor that plans 04 and 05 wire in — this plan establishes that host + the detail rendering + the 404-is-error five-state behavior.

Output: `ItemDrawer.tsx` (sheet, `?item`-synced, GET item, rendered fields + RawJsonViewer + CopyableId + an action-region host stub), MemoryPage wiring to mount it, and component tests for open-on-param, field rendering, 404→error, and close-clears-param.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-memory-console/02-RESEARCH.md
@.planning/phases/02-memory-console/02-UI-SPEC.md
@.planning/phases/02-memory-console/02-VALIDATION.md
@.planning/phases/02-memory-console/02-01-SUMMARY.md
@.planning/phases/02-memory-console/02-02-SUMMARY.md
</context>

<interfaces>
<!-- From 02-01 -->
From web/src/features/memory/api/queries.ts:
  useItemQuery(id) → { data?: MemoryItem, isLoading, error }  // enabled only when id set
  memoryKeys.item(id)
From web/src/features/memory/api/schemas.ts:
  memoryItemSchema type: { memory_id, kind, version, content, tags?, source?, category?, importance?, pinned, disabled }
  parseGatewayError error → err.httpStatus (404 → not_found)

<!-- From 02-02 -->
From web/src/features/memory/hooks/useMemorySearchParams.ts:
  item getter + setter (clear by setting item: undefined)

<!-- Phase-1 substrate -->
From web/src/components/primitives/RawJsonViewer.tsx: <RawJsonViewer data={unknown} label?={string} />  // collapsed by default, copy-to-clipboard, escaped text
From web/src/components/primitives/CopyableId.tsx: <CopyableId id={string} />
From web/src/components/primitives/FiveStateWrapper.tsx: FiveStateProps { loading, error?, empty?, partial?, children }
From web/src/components/ui/: shadcn `sheet` (add via `npx shadcn add sheet` — RESEARCH A1: `sheet` not `drawer`, official registry, no vetting gate), button, badge, separator, scroll-area

<!-- UI-SPEC -->
Drawer width fixed 480px (desktop side-sheet); md (16px) content padding; mono for id/version/tags/timestamps; sans for content
IC-2: drawer five-state loading→ready; 404 → error INSIDE the drawer (not empty)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ItemDrawer — sheet synced to ?item, GET item, rendered fields + RawJsonViewer, 404→error</name>
  <files>web/src/features/memory/components/ItemDrawer.tsx, web/src/features/memory/MemoryPage.tsx, web/src/features/memory/components/ItemDrawer.test.tsx</files>

  <read_first>
    - `.planning/phases/02-memory-console/02-UI-SPEC.md` IC-2 (drawer synced to URL; sheet slides over list; reload reopens; back closes; five-state loading→ready; 404→error inside drawer), "New Surfaces" S3 (sheet + raw-JSON viewer + copyable-id; loading/error/ready, empty n/a), Spacing (drawer 480px, md padding), Typography (mono for id/version/tags), Registry Safety (`sheet` not `drawer`)
    - `.planning/phases/02-memory-console/02-RESEARCH.md` "Item record shape — grounds the zod schema" (GetMemoryItemResponse fields), D-04/D-05 (drawer over route; ?item param), "Vertical slices" Slice B, the recommended feature structure (ItemDrawer.tsx)
    - `.planning/phases/01-foundation/01-05-PLAN.md` Task 2 (RawJsonViewer props {data,label}; CopyableId props {id})
    - `.planning/phases/02-memory-console/02-02-SUMMARY.md` (useMemorySearchParams item getter/setter; how ResultsTable sets ?item)
  </read_first>

  <behavior>
    - Test (open on param): rendering MemoryPage with router search `{item:'mem_1'}` (and context set) mounts the drawer and triggers GET /api/memory/items/mem_1 (fetch mock records the call)
    - Test (rendered fields): when the GET resolves to the item fixture, the drawer shows the memory_id (via CopyableId), kind, version, content text, and the tags
    - Test (raw JSON): the drawer includes the Phase-1 RawJsonViewer with the full item object (collapsed by default; the raw JSON content is not in the DOM until expanded)
    - Test (loading): while the GET is in flight, the drawer shows the loading state (not a blank panel)
    - Test (404 → error): when GET returns 404 not_found, the drawer renders the ERROR state ("404 from memory-gateway — ...") inside the drawer, NOT an empty state
    - Test (close clears param): activating the drawer close control clears ?item from the router search params AND does not trigger a recall fetch (recall call count unchanged)
    - Test (no item → no drawer): with no ?item param, the drawer is not mounted and no GET item call fires
  </behavior>

  <action>
    Create `web/src/features/memory/components/ItemDrawer.tsx`. Read the open item id from `useMemorySearchParams().item`. Render a shadcn `Sheet` whose `open` is `Boolean(item)` and whose `onOpenChange(false)` clears the param (set `item: undefined` via the search-params setter) — this makes browser-back close it and reload reopen it (URL is the source of truth, D-05). The `SheetContent` is a fixed 480px side panel (right side) with md padding.

    Inside, drive `useItemQuery(item)` (enabled when item set). Wrap the body in `<FiveStateWrapper>`: isLoading→loading; error→error (map `err.httpStatus`/message to `{status} from memory-gateway — {message}`; a 404 lands here as the error state per IC-2 — there is NO empty state for the drawer since an id implies existence); else ready → the detail body. The detail body renders, using UI-SPEC typography: a header with `<CopyableId id={item.memory_id} />` (mono) + the pinned/disabled badges (reuse `StateBadges` from 02-02); a field grid of `kind` (Label), `version` (mono), `source`/`category` (muted), `importance` (if present), `tags` (mono chips), and `content` (sans Body, full text rendered as a TEXT node — no dangerouslySetInnerHTML); then a `Separator`; then `<RawJsonViewer data={item} label="Raw JSON" />` (Phase-1 primitive, collapsed by default, copy-to-clipboard). Below the detail, render an **action-region host**: a "Patch" button and a lifecycle action set as PLACEHOLDERS — render the buttons but leave handlers as no-op stubs with TODO comments referencing plans 02-04 (Patch/write/patch editor) and 02-05 (pin/unpin/disable/enable/delete + confirms + OCC). Do NOT implement the mutations here; this plan only establishes the host. Keep a clearly-marked extension point (e.g. an `<LifecycleActions item={item} />` placeholder import that plan 05 fills, or a commented region) so plans 04/05 wire in without restructuring.

    Update `web/src/features/memory/MemoryPage.tsx` to mount `<ItemDrawer />` once (inside the context-gated branch, alongside the results region) so it overlays the list when ?item is set. The gate still applies — the drawer only matters when context is set and a row was opened.

    Create `web/src/features/memory/components/ItemDrawer.test.tsx` covering all seven behavior cases using the Phase-1 router test pattern (createMemoryHistory with initialEntries including `/memory?item=mem_1`), a test QueryClientProvider + OperatorContextProvider (context set), and the 02-01 fetch mock (item fixture + a 404 case). Assert the GET path, field rendering, the RawJsonViewer collapsed-by-default behavior, the 404→error mapping, and that closing clears ?item without a recall call.
  </action>

  <verify>
    <automated>cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-console/web && npx vitest run src/features/memory/components/ItemDrawer.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/features/memory/components/ItemDrawer.test.tsx` exits 0; all seven behavior cases green
    - `grep -c 'useItemQuery' web/src/features/memory/components/ItemDrawer.tsx` returns >= 1 (GET item via 02-01 hook)
    - `grep -c 'RawJsonViewer' web/src/features/memory/components/ItemDrawer.tsx` returns >= 1 and `grep -c 'CopyableId' ...` returns >= 1 (Phase-1 primitives reused, not rebuilt)
    - `grep -c 'Sheet' web/src/features/memory/components/ItemDrawer.tsx` returns >= 1 (shadcn sheet, not a full route)
    - `grep -c 'dangerouslySetInnerHTML' web/src/features/memory/components/ItemDrawer.tsx` returns 0 (content/raw JSON as text — V5)
    - `grep -c 'item:' web/src/features/memory/components/ItemDrawer.tsx` returns >= 1 (close clears the ?item param)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>ItemDrawer opens from ?item, fetches GET item, renders fields + tags + content (as text) + pinned/disabled badges + CopyableId + the Phase-1 RawJsonViewer; loading and 404→error five-states correct; closing clears ?item without re-running recall; an action-region host stub is in place for plans 04/05. Component tests green incl. URL-sync and 404→error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET item strings (content/tags/raw JSON) → DOM | Untrusted memory content rendered in the drawer + raw-JSON viewer; React text escaping is the XSS boundary |
| ?item URL param → GET item request | Operator-controlled id drives the GET; the BFF allowlist confines it to /api/memory/items/{id} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02B-01 | Tampering / Elevation (stored XSS) | ItemDrawer content/tags + RawJsonViewer | mitigate [BLOCKING] | Content/tags rendered as React text nodes; RawJsonViewer (Phase-1) renders JSON.stringify into a <pre> text node — never dangerouslySetInnerHTML. Acceptance grep asserts zero dangerouslySetInnerHTML in ItemDrawer |
| T-02B-02 | Tampering | ?item id drives upstream GET | mitigate | The id is sent only as a path segment to the allowlisted /api/memory/items/{id} (BFF allowlist, Phase-1 BFF-01); the console never constructs an arbitrary upstream URL (no SSRF) |
| T-02B-03 | Info disclosure | drawer error state + raw-JSON disclosure | accept | 404/error shows real upstream status+message + raw error via the Phase-1 viewer (BFF-04 passthrough); internal operator tool |
| T-02B-SC | Tampering | shadcn `sheet` block add | accept | Official shadcn registry (UI-SPEC Registry Safety / RESEARCH A1: `sheet` is official, no vetting gate); copy-in code, no npm runtime dep |
</threat_model>

<verification>
## Wave 3 Phase Checks

1. `cd web && npx vitest run src/features/memory --run` — Slice-A + Slice-B tests PASS
2. `cd web && npx tsc --noEmit` — exits 0
3. `grep -c 'dangerouslySetInnerHTML' web/src/features/memory/components/ItemDrawer.tsx` — 0 (V5)
4. `grep -c 'RawJsonViewer' web/src/features/memory/components/ItemDrawer.tsx` — >= 1 (reuses Phase-1 viewer)
5. ItemDrawer.test asserts 404 → error state (not empty) and close → ?item cleared with no recall call
</verification>

<success_criteria>
- MEM-02: ?item opens a sheet drawer over the list; GET item renders fields + raw JSON; memory_id via CopyableId
- D-04/D-05: drawer not a full route; ?item URL-synced (reload reopens, back closes, shareable)
- IC-2 five-state: loading → ready; 404 → error inside the drawer (not empty)
- Closing clears ?item without re-running recall (D-09)
- Content/tags/raw JSON rendered as text nodes (no XSS); action-region host ready for plans 04/05
- All Wave-3 tests + tsc green
</success_criteria>

<output>
Create `.planning/phases/02-memory-console/02-03-SUMMARY.md` when done
</output>
